# Burgers 补训准备

## 启动顺序

当前只完成代码准备与 CPU 合成数据检查，**未启动 Burgers 训练，也未用真实 Burgers 数据进行优化**。

四个已有模型的 60 个主实验单元必须先全部完成。Burgers 的缓存准备、GPU 训练测速和训练入口都会检查 `reports/first60_complete.json`、60 个单元的样本 ID、每批收据和预测文件哈希。单个标志文件不能绕过这一检查。

## 模型与训练

- 复用服务器 CoCoGen 的 UNET1，输入/输出改为 1 个完整时空通道，数据顺序为 `[batch,1,time,space]`，分辨率 128×128，`n_feat=256`、`pool_size=4`。
- 使用基础 score 模型，不添加仅接收 PDE 常数标签的 ControlNet。
- VP 的 `beta_min=1e-4`、`beta_max=10`，时间从 `(1e-5,1]` 均匀采样。
- 损失保持原 CoCoGen 的逐像素求和：`mean_batch(sum_pixels((score*std+noise)^2))`。同时记录除以 16,384 后的每像素损失，避免把三/四位数损失直接当成预测误差。
- AdamW：学习率 `1e-4`、weight decay `0.01`；float32，TF32 开启。
- 计划两张 GPU 做 DDP，每卡 batch 最多 32、全局 batch 最多 64。实际训练前先测 batch 1→4→8→16→32 的前向、反向和优化器峰值显存，根据实测峰值保守筛选下一档，采用实际通过测速的最大安全批量（至少 8）。最终批量及每轮耗时估计写入训练配置。
- 每轮重新打乱训练样本。至少训练 300 轮、最多 1,200 轮；每 10 轮检查固定验证面板，连续 20 次检查未达到相对于参考值 0.2% 的改善时，在满足至少 300 轮后停止。
- 每轮保存可恢复的 `last.ckpt`；固定验证损失改善时保存 `best.ckpt`；每 100 轮保留独立快照。保留模型、优化器、各 rank 的 Python/NumPy/PyTorch CPU/CUDA 随机状态、归一化统计、代码和数据标识。

## 数据隔离

训练样本来自五个原始训练分片，共 50,000 个。均值与 sample std 从训练样本恢复，归一化除以 `std+1e-8`；缓存建立时逐分片复核原始 float32 字段哈希。

三个测试文件 ID 段的用途严格分离：

| ID 文件内的样本行 | 用途 |
|---|---|
| `0..999` | 正式主实验；ID、smooth、rough 各两种观测设置 |
| `1000..1095` | 采样配置的选择与独立验证 |
| `1096..1351` | 仅选择训练 checkpoint 的固定验证面板 |

后两组是从未进入正式评估的 ID 测试文件行中留出的面板，不应称为原始训练集。固定验证分别检查均匀随机时间与 `t=0.001/0.01/0.1/0.5/1`，报告每像素和求和损失；不会把这些数据加入优化器更新。

## 已验证与尚待验证

CPU 合成数据测试已通过：第一阶段未完成时拒绝启动、缓存样本访问不改写原数据、score matching 目标与损失尺度正确、模型/优化器/随机状态恢复后的下一步更新逐值一致、固定验证可重复、按 rank 分片后字段/时间/噪声保持一致、训练保存的单通道 checkpoint 由采样网络加载后输出逐值一致。检查收据绑定训练源文件哈希，保存在 `docs/execution_20260915/validation_burger_training_cpu.json`。

真实 128×128 网络的反向显存、双 GPU 通信、完整数据训练速度和训练后的六个采样评估单元尚待执行。Burgers 总耗时以那时的测速更新，和用户要求的四模型约一天正式评估分开记录。

DDP 与序列化依据 [PyTorch 2.8 分布式文档](https://github.com/pytorch/pytorch/blob/v2.8.0/docs/source/distributed.md) 与 [DDP 说明](https://github.com/pytorch/pytorch/blob/v2.8.0/docs/source/notes/ddp.rst)，并将服务器实际 PyTorch 版本写入训练记录。

## 接续与正式评估

2026-09-15 11:10 CST 核查：`cocogen_burger_pipeline` 的进程存活，状态为 `waiting_for_first60`，未占用 GPU。调度器使用固定 Git 检出 `code_burger`（`36875e0412361a7d2d449a08675b9b143f1f401b`）。它依次接续数据准备、训练显存测速、双卡训练、采样校准、双卡六单元评估和 66 单元汇总；各阶段使用独立 tmux、日志和退出码。

11:15 CST 更新：同步修正四模型校准中发现的完成日志字段重名问题，等待中的协调器已切换到 `code_burger_v2`（`bf94ef703f499fb50e099ef9af34849e5390e344`）。仍为 `waiting_for_first60`，训练代码和已通过的 CPU 检查哈希未变化。

训练完成后比较 `best.ckpt` 与 `last.ckpt`，在最多 500 NFE 的配置中校准。校准样本与正式评估隔离，冻结配置后用独立 64 例/设置验证。

Burgers 正式输入仍来自 FM4PDE 的历史归档。六个单元的 `zeta_obs_a` 均为 0，因此仅应用解场 mask：每个分布分别使用随机 500 个点和 5 个完整时间列。历史归档同时保存的系数候选 mask 不参与观测；合并两个 mask 会额外泄露真值，不能采用。

训练完成不等于整个任务完成。六个单元各完成 1,000 个样本、所有预测文件通过哈希核验并写出 `reports/all66_complete.json` 后，才进入最终效果分析与归档清理。
