# CoCoGen 采样核查与 FM4PDE 主实验执行协议

## 范围与顺序

用户已明确：先完成 Darcy、Poisson、Helmholtz、NS 四个已有模型的 **60 个评估单元，每单元 1,000 个样本**；之后补训 Burgers，再完成它的 6 个单元。

**当前进度（2026-09-16 05:41 CST）：已完成并核验 55/60 个正式单元，即 Darcy、Poisson、Helmholtz 各全部 15 个，NS 完成 10 个，共 55,000 次完整单元样本评估。前三者宏平均相对 L2 分别为 CoCoGen / FM4PDE：33.61% / 16.07%、40.34% / 20.75%、42.61% / 23.47%。新增 NS ID 稀疏联合重建的两场等权误差为 33.72% / 13.06%；初始涡量和末态场均落后。剩余 5 个 NS 单元，两张 A100 正常并行采样；尚不产生 NS 或四模型总体宏平均。9 月 16 日 02:01 按实际采样速度估算约 07:14 完成纯计算，给剩余时间添加 15% 余量后约 08:00。Burgers 仍在等待前 60 单元全部完成。**

已完成 PDE 的全部目标场结果、结论和耗时另见 [Darcy 完整评估总结](darcy_complete_results_20260915.md)、[Poisson 完整评估总结](poisson_complete_results_20260916.md) 与 [Helmholtz 完整评估总结](helmholtz_complete_results_20260916.md)。

跨 PDE 的可复现比较表已在 [54 单元快照](execution_20260915/comparison_54cells/comparison.md) 上生成，包含已完成 PDE 的任务/分布均值及全部已完成目标场。`cocogen_eval/summarize_main_results.py` 读取不可变指标快照及绑定它的归档复核覆盖文件，核对目标场、单元、PDE 与总体权重，默认拒绝未完成快照。真实 54 单元数据与此前三份完整 PDE 报告的均值、分布、胜出单元数和耗时一致，NS/总体均值仍为空，见 `validation_comparison_report_54cells.json`。该工具只制表，不重新读取预测或修改正式采样；完整 60/66 单元路径尚待真实结果验证。

针对稀疏引导的补充核查见 [当前方法与论文的关系](sparse_guidance_audit_20260916.md)：Darcy 全部九个稀疏单元、9,000 次任务评估的观测点均逐值满足，逐例误差通过 CPU float64 重算；正文观测条件模型与本次附录 D 短采样适配的区别单独列明。

用户另要求测试 2,000 步并增加 RePaint。[Darcy 长采样诊断](darcy_long_sampling_diagnostic_20260916.md) 已完成全部 72 次生成、独立指标复核、24 页对照图、197 主归档和源码独立恢复。八个校准样本上，100 步/r=4、2,000 步/r=4、2,000 步/r=10 的三任务平均误差分别为 32.70%、33.17%、31.54%；r=10 的实测采样成本为基线的 37.95 倍。四卡并行采样约 18.13 分钟。改善幅度较小，属于单种子 ID 小样本诊断；正式配置及 Burgers 接续顺序保持冻结。

2026-09-15 10:37 CST 更新：用户要求四模型正式评估约 1 天，优先效果与耗时的平衡。原 4,000/8,000 NFE 长采样的自动接续已停止；已完成的长配置校准结果保留。当前改为 **每样本最多 500 NFE** 的短步数/重采样配置校准，在 60×1,000 个样本和相同观测协议下优化效果。正式评估尚未启动，Burgers 尚未训练。66 单元输入核验与恢复测试已通过。检查与首轮测速的原始 JSON 留存在 `docs/execution_20260915/`。

主目录：`/research_data/users/zhangxifeng/C01Python/FM4PDE/outputs/cocogen_main_20260915`。

## 论文依据与适用范围

参考 Jacobsen、Zhuang、Duraisamy 的 [CoCoGen，arXiv:2312.10527v2](https://arxiv.org/abs/2312.10527v2)，特别是第 3 节和附录 D。

论文区分了两条条件生成路线：

1. 正文中的观测条件模型：用传感器观测训练条件网络。
2. 附录 D 的 data imputation：用已训练的 score 模型、观测量的前向 VP 扩散边缘分布、反向 SDE 和 RePaint 完成条件生成。

197 上已有 ControlNet 权重训练时仅输入常数 PDE 编号，不能解释为学习了观测条件的正文模型。本次采用附录 D 路线，同时在独立校准集上比较基础模型与这些 ControlNet 权重。

附录 D 使用 Euler–Maruyama、2,000 个时间间隔、最后 50 步物理修正及 10 步后处理。文中的 `r` 是每个时间间隔 **额外** 的前向/反向循环数。因此本实现每样本 NFE 为 `steps × (1 + repaint)`。例如 2,000 步、`repaint=1` 为 4,000 次网络调用。

## 已发现的问题与处理

旧服务器源文件逐文件保存于 `reference/server197_20260915/`，原服务器实验文件保持原位。

| 问题 | 影响 | 本次处理 |
|---|---|---|
| 在时间网格最后一点继续减一步 | 前向方差可能在负时间开方，出现 NaN | 以正时间评估网络，严格走到 0；观测在 0 时恢复原值 |
| RePaint 分支错误调用 `self.g` | 部分分支运行失败 | 直接使用 VP 扩散系数 |
| PF ODE 构造器位置参数错位 | 重采样次数可能被错误放大 | 附录 D 采样器独立实现，参数以名称记录 |
| 物理采样器覆盖传入条件并清零 | 非 Darcy 的 ControlNet 与训练条件不符 | 传入各 PDE 训练时的固定编号 |
| 旧 `[p,k]` 假设与服务器 `[a,u]` 数据顺序不符 | 观测、指标和物理修正通道可能颠倒 | 整个评估采用 `[a,u]`；Burgers 为单通道完整时空场 |
| 用 pressure 的均值/方差还原整个张量 | 系数场与解场的物理单位错误 | 分通道恢复原训练统计，分通道归一化/反归一化 |
| 配置中给出的物理修正步长未被使用 | 调参无效、结果难复现 | 将修正次数、更新上限和回溯次数全部写入配置与收据 |
| float32 上以 `1e-8` 作数值差分，且只用对角导数 | 消减误差、缺少邻点对总残差的贡献 | 对完整离散残差平方和进行自动微分 |
| 用带符号最大梯度归一化，缺少零梯度保护 | 步长异常、修正可能增大残差 | 用最大绝对梯度、零梯度保护和逐样本回溯下降 |
| 原作者问题与 FM4PDE 的源项、网格和边界不一致 | 残差不能代表当前数据的 PDE | 冻结 FM4PDE 的物理算子和边界配置，仅复用评估定义 |
| 旧样例没有完整样本/观测/权重关联 | 无法构成可配对主实验 | 使用归档的正式输入、观测 mask、样本 ID，并保存权重与结果哈希 |

物理修正默认只更新解通道，这与论文 Darcy 方法一致。对全观测解的逆问题，该修正不会更改观测值，也不会额外改变系数通道。`all` 通道和精确 VP 重加噪接口被明确视为扩展，本轮默认网格未启用。

最大绝对梯度归一化、自动微分和回溯是对残差下降的数值实现修正。FM4PDE 物理算子是针对当前基准的适配。因此应将结果描述为“基于 CoCoGen 附录 D 的修正与适配”，不能声称逐行复现原论文实验。

## 输入与公平性

- 统一字段名 a 表示输入场，u 表示解场；a 在 Darcy 中是系数场，在当前 Poisson 和 Helmholtz 算子中是源项场，不能把所有 PDE 的 a 都解释为材料系数。
- 正式样本、掩码、分布、任务复用 FM4PDE 的冻结输入目录 `outputs/pretrained/resume_main_20260911/evaluation_inputs`。
- 四个 PDE 各有 ID / smooth / rough × sparse_forward / sparse_inverse / sparse_joint / full_forward / full_inverse，共 60 单元。
- 正式 ID 为 `0..999`；NS 为 `2000..2999`。稀疏观测每个活动场 500 点，全观测为 128×128。保留原主实验中 shared-mask 设置的例外，直接读取历史 mask。历史文件同时保存两个场的候选 mask；FM4PDE 在观测损失中按 task 屏蔽非活动场。本适配在输入端做同样的屏蔽，避免 forward 额外看到解观测，或 inverse 额外看到系数观测。
- 原训练归一化从各 PDE 的 50,000 个训练样本恢复；不使用正式测试集统计。保留 sample std 的 correction=1 和原训练的 `std+1e-8`。
- 每个 PDE 另取 ID 文件的 `1000..1095` 作为校准与验证池。这些是未进入正式评估的测试文件行，应明确称为“独立留出的 ID 校准样本”，不是训练集。
- `1000..1031` 用于配置选择；`1032..1095` 在配置冻结后用于验证，不根据后者重新调参。不同分布使用同一 PDE 的冻结配置。
- 模型采样接口只接收观测值和 mask；未观测位置在进入采样器时再次清零。完整真值仅在采样结束后计算误差。
- 随机数按 PDE、seed、样本 ID 派生独立流；改变分批不会改变该样本的随机数。算子浮点舍入仍可能随批量变化，正式批量与 TF32 状态会留档。
- 指标为物理空间逐样本相对 L2，再对 1,000 个样本汇总；同时保留每样本误差、配对 FM4PDE 差值、PDE 残差、真值残差、实际 NFE 和耗时。
- NS 沿用主实验的 endpoint_secant 残差，属于两端点近似，不能解释为完整轨迹的 PDE 一致性。

## 校准与资源

预先定义的粗筛比较基础/ControlNet 权重、100/500/2,000 步、不同额外 RePaint 次数；随后检查物理修正是否有收益以及 `2e-4` 与 `2e-3` 更新上限。粗筛每设置 4 个样本，前两名扩大到每设置 32 个样本，赢家再验证每设置 64 个新样本。选择指标为五种观测设置的目标场相对 L2 宏平均。

CPU 准备任务已完成。GPU 校准分别在以下 tmux 中运行：

- `cocogen_calibrate_ph`：GPU 0，Poisson 后 Helmholtz。
- `cocogen_calibrate_dn`：GPU 1，Darcy 后 NS。

上述长配置校准组和 `cocogen_main_ph`、`cocogen_main_dn`、`cocogen_aggregate60` 的旧接续已停止。新的 `cocogen_cost_ph` / `cocogen_cost_dn` 使用固定 Git 检出 `code_cost`，在最多 500 NFE 内比较基础/ControlNet、100/250/500 个时间步和额外重采样；再用 32 例/设置选择、64 个独立新样本/设置验证。正式采样将根据四个 PDE 的完整验证吞吐分配两卡，并检查含余量的预计耗时是否在 24 小时内。训练 Burgers 的前置条件仍是 `first60_complete.json`，不是校准或任务启动完成。

已验证的 Darcy / Poisson：基础网络与 ControlNet 在三个时间点与服务器原类输出逐值相等；小型回归验证了时间区间、NFE、精确观测、无隐藏真值依赖、随机数分批独立性、仅解通道修正、残差下降及邻点梯度。

首轮 A100 float32、TF32 开启的网络测速如下；不包含完整采样的额外操作：

| 模型 | batch | 每次网络调用 | 峰值分配显存 | 4,000 NFE 时每卡每小时样本数 |
|---|---:|---:|---:|---:|
| 基础模型 | 32 | 约 0.108 s | 约 4.26 GB | 约 266–268 |
| ControlNet | 32 | 约 0.152 s | 约 5.02 GB | 约 188–190 |

batch 64 的吞吐提升不到约 1%，因此使用 batch 32。两卡同时校准时已观察到 100% GPU 利用率，显存有足够余量。更多占用显存不会自动缩短计算时间。

原先的 5–7 天估算对应 4,000 NFE，不是最终选定时长。按用户新要求，当前限制为最多 500 NFE；同一测速下两卡纯网络计算约 **14–20 小时**，另需物理修正和 I/O。100 NFE 约为 3–4 小时。将使用完整验证的实际吞吐确认 24 小时目标，而不直接套用网络测速。Burgers 在前 60 单元完成后才补训，单独估算和记录时间。

最初的 24 小时调度器固定在 `code_main24`（`3f992760f155e7a75af7ee2d9eb02237948652aa`），枚举四个完整 PDE 任务在两卡上的分组。它于 11:32 CST 在等待状态下停止，由下面的分单元调度替代；已运行的校准进程保持原样。新旧方案都在实测总耗时上添加 **15% 余量**；含余量的预测超过 24 小时则不启动正式采样，记录 `needs_lower_cost` 供降低采样成本后继续。正式评估的计时从两卡主任务启动算起；前期校准与后续 Burgers 单独计时。

2026-09-15 11:16 CST：Darcy 和 Poisson 已完成独立 64 例/设置验证，15,000 个正式样本的推算分别为 **10.46 和 10.27 GPU 小时**，尚不代表四模型整体预算已经通过。两者都选中 ControlNet、100 时间步、额外 RePaint 4 次、物理更新上限 `0.002`。验证记录已同步到 `docs/execution_20260915/selected_{darcy,poisson}.json`。

两者在保存完整结果后，曾因完成日志的重复 `stage` 字段退出。修正仅涉及日志输出和启动脚本，采样实现哈希不变；已有验证预测逐文件校验后复用，未重新采样。旧失败日志及退出码保存在服务器 `logs/archive/calibration_log_collision_20260915_1114`。当前校准使用 `code_cost_v2`（`bf94ef703f499fb50e099ef9af34849e5390e344`）；Helmholtz 和 NS 的网络一致性与采样检查已通过，短配置校准已接续运行。

### 当前两卡分工

`cocogen_cell24_schedule` 使用固定检出 `code_cell24`（`ae7cd742211afdfbc4ee0859671b9d75fb613283`）。正式任务为 `cocogen_cell24_gpu0` / `cocogen_cell24_gpu1`，已在四项校准及独立验证全部完成后，于 2026-09-15 11:51:55 CST 启动。

新方案以评估单元为分配单位：每个 PDE 的 15 个单元按该 PDE 实测总耗时的 1/15 估算，枚举两卡之间的单元数量分配，使预计结束时间尽量相近。这样允许某个 PDE 跨两张卡执行。每张卡按 PDE 连续运行分配到的单元，仅加载一次相应模型；每个单元仍调用已验证的 `evaluate_cell`，使用原始样本 ID、观测 mask、冻结配置、批量 32 和 1,000 个样本。

分工和四个配置文件的哈希写入 `protocol/budget_24h.json`。每个 worker 只保存自己的进度；两者都完成且单元集合与分工一致后，才生成四个 PDE 的完成记录并核验汇总全部 60 单元。Burgers 的前置条件保持不变。

CPU 调度检查已通过，记录在 `docs/execution_20260915/validation_cell_budget.json`：覆盖 60 个单元恰好执行一次、每 PDE 15,000 样本、拒绝缺失/重复完成记录、冻结配置哈希检查，以及含 15% 余量的超时预算拒绝。该检查使用合成单元结果，只证明调度和完成判定；GPU 采样由已有各 PDE 检查与 evaluator 检查覆盖。报告中的分配耗时示例是测试用数据，不是正式耗时预测。

## 正式启动记录

四个 PDE 都选中 100 个时间步、额外 RePaint 4 次（500 NFE）、最后 50 个时间步的物理修正及 10 步后处理、更新上限 `0.002`。物理修正在每次 RePaint 反向访问后执行，因此实际每样本有 260 次修正尝试。各 PDE 的选定 checkpoint 阶段和独立验证如下；宏平均是每种任务 64 个独立样本上的目标场相对 L2，不是正式 1,000 样本主实验结论。

| PDE | checkpoint 阶段 | 独立验证宏平均相对 L2 | 15,000 样本预计 GPU 小时 |
|---|---|---:|---:|
| Darcy | ControlNet | 0.34780 | 10.4583 |
| Poisson | ControlNet | 0.36253 | 10.2725 |
| Helmholtz | 基础模型 | 0.37199 | 7.2335 |
| NS | ControlNet | 0.36287 | 10.5575 |

两卡分配如下，32/28 个单元的差异用于平衡不同模型的速度：

| GPU | Darcy | Poisson | Helmholtz | NS | 单元数 | 预计小时 |
|---|---:|---:|---:|---:|---:|---:|
| 0 | 4 | 7 | 14 | 7 | 32 | 19.2608 |
| 1 | 11 | 8 | 1 | 8 | 28 | 19.2610 |

包含 15% 余量后为 **22.1501 小时**；整 PDE 分组方案为 23.8404 小时。两者均为启动前预测，实际耗时将由主任务记录。完整分工和配置哈希保存在 `docs/execution_20260915/budget_24h_launch.json`；四项选定配置和独立验证保存在同目录的 `selected_<pde>.json`。

`validation_main_launch.json` 核验了两张卡各前两批、共 128 个正式样本：预测文件哈希、落盘数据与收据一致，样本 ID 与历史有效 mask 一致，checkpoint/采样实现匹配冻结选择，预测有限且观测值精确保留，实际为 500 NFE 并到达时间 0，保存的逐样本物理相对 L2 可在 CPU 重新计算。后续仍需完成全部 60,000 个样本并核验每个单元。

### 首次调用的显存峰值

正式首批每卡记录到 **76.285 GiB** 的临时分配峰值，第二批降至 **4.742 GiB**；第二批约耗时 79–80 秒/32 样本，`nvidia-smi` 的持续占用约 7,287 MiB。前面的网络测速表是在预热后重置峰值计数，因此不能代表进程启动峰值。

本实现开启 `cudnn.benchmark`，而 [PyTorch 2.8 的卷积搜索源码](https://github.com/pytorch/pytorch/blob/v2.8.0/aten/src/ATen/native/cudnn/Conv_v8.cpp) 会为候选执行计划申请临时工作区。这与观测到的首次高峰、后续回落一致，属于根据源码和实测的推断，未进行调用栈归因。当前两卡仅有本任务计算进程，采样持续正常；重启或评估新的张量形状时，不能把 4.742 GiB 直接当作完整启动显存预算。

## 完成判据与结果

### 已完成的八个 Darcy 单元，每单元 1,000 个样本

截至 2026-09-15 14:40 CST，Darcy ID 的五种任务，以及 rough 的完整观测正问题、完整观测反问题、稀疏正问题已完成，均为各自分布的样本 `0..999`。CoCoGen 与 FM4PDE 使用相同样本编号和历史有效观测 mask；指标收集器核验了全部 256 个预测文件的哈希、样本编号完整性、冻结配置，以及逐批误差重新汇总和历史指标配对。指标为物理空间逐样本相对 L2 的平均值，下表以百分数表示，越低越好。

| 分布、任务与目标场 | CoCoGen | FM4PDE |
|---|---:|---:|
| ID，完整系数观测，预测解 u | 0.70% | 7.78% |
| ID，完整解观测，反推系数 a | 49.16% | 19.05% |
| ID，稀疏系数观测，预测解 u | 19.99% | 8.28% |
| ID，稀疏解观测，反推系数 a | 62.43% | 19.88% |
| ID，稀疏联合，系数 a | 53.03% | 26.07% |
| ID，稀疏联合，解 u | 16.10% | 4.15% |
| rough，完整系数观测，预测解 u | 0.74% | 12.46% |
| rough，完整解观测，反推系数 a | 55.54% | 38.26% |
| rough，稀疏系数观测，预测解 u | 18.00% | 12.87% |

这八个单元显示出明显的任务差异：CoCoGen 在完整系数观测下预测解更准，在反问题、稀疏正问题和稀疏联合设置下的误差都更大。当前仅完成 8/60 个单元，不能推广为四模型或全部 60 单元的结论。双方使用各自既定采样配置，因此这是当前实验配置的效果比较。所有 CoCoGen 配置在正式评估前已冻结，未根据正式结果重新选择。完整系数观测正问题仅将未知解 u 作为目标，不把已观测系数 a 的零误差计入效果比较。

Darcy ID 五种任务已齐备，按任务等权、联合任务两场先等权平均，CoCoGen 的宏平均相对 L2 为 **33.37%**，FM4PDE 为 **14.02%**。这是同一分布的完整五任务结果，不是 Darcy 三分布或四模型总体指标。五种任务复用同一批 1,000 个样本，不能将它们当作 5,000 个独立样本估计不确定性。结果保存在 `docs/execution_20260915/darcy_id_complete_comparison.json`，绑定独立保留的 `metrics_first60_8cells.json` 快照及其哈希。

八个单元的纯采样耗时为 41.37–42.05 分钟，与启动前的 Darcy 单元估计 41.83 分钟接近。第一张 GPU 已转入 Poisson，并于 14:39 CST 确认首批 32 例完成，耗时 78.83 秒。仍保留四模型含余量共 22.15 小时的启动前预测，待其余模型实跑后更新。这八个完整单元的原始均值、误差比、逐例胜率、耗时和源文件哈希保存在 `docs/execution_20260915/metrics_first60_8cells.json`。首两个单元独立检查保留在 `first_two_complete_cells.json`，此前前 512 例预览保留在 `early_darcy_512.json`；以上完整结果取代该预览。

### 15:22 CST 更新：首次完整 Poisson 结果与第九个 Darcy 单元

新增两个单元均已完成各自的 1,000 个正式样本，并通过指标收集器的样本编号、冻结配置、逐批预测哈希和误差配对检查。累计 10/60 个单元、11 个目标场比较、320 个预测文件通过核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Poisson ID，完整观测正问题 | u | 6.20% | 7.99% | 70.2% | 41.15 |
| Darcy rough，稀疏反问题 | a | 62.69% | 38.96% | 0.0% | 41.94 |

Poisson 在这个完整观测正问题上的平均误差降低约 22.4%，尚不能代表其余 14 个单元。Darcy rough 的稀疏反问题误差约为 FM4PDE 的 1.61 倍，在此次 1,000 个样本中均更大，与之前 Darcy 反问题较弱的结果一致。当前所有 PDE 都还缺少单元，因此不报告 PDE 三分布总体或四模型总体宏平均，也不根据正式结果重新选择采样参数。

新增单元的耗时仍接近启动前的实测估计，保留四模型 22.15 小时的含余量预测；Burgers 的训练与评估另行计时。两张卡已接续 Poisson ID 完整观测反问题和 Darcy rough 稀疏联合重建。

本次快照生成于 2026-09-15 15:22:33 CST，保存在 `docs/execution_20260915/metrics_first60_partial.json` 与同名 `_targets.csv`；JSON 的 SHA256 为 `61c681c5befb22f06fea720ac8376206ee734ac80c734ff00364cc83b6cb2765`。后续更新同名快照时，历史版本通过 Git 保留；八单元快照及其绑定的 Darcy ID 五任务比较继续独立保留。

### 16:04 CST 更新：Poisson 反问题与完整 Darcy rough 五任务

Poisson ID 完整观测反问题和 Darcy rough 稀疏联合重建均已完成各自的 1,000 个正式样本。指标收集器累计核验了 12 个单元、14 个目标场比较、384 个预测文件，样本编号、冻结配置、逐批哈希和误差配对均通过。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---|---:|---:|---:|
| Poisson ID，完整观测反问题 | a | 30.42% | 24.55% | 6.0% |
| Darcy rough，稀疏联合重建 | a | 56.15% | 40.91% | 0.4% |
| Darcy rough，稀疏联合重建 | u | 14.81% | 6.48% | 0.3% |

Poisson 的完整观测反问题平均误差约高 23.9%，与其完整观测正问题更优的结果存在任务差异；剩余 13 个单元仍待完成。两个新增单元的纯采样耗时分别为 41.17 和 41.93 分钟，仍接近启动前估计。

Darcy rough 五任务现已齐备。沿用 ID 子组的计算口径，先在联合任务内部平均 a/u 两场，再对五种任务等权平均，得到以下两个完整分布子组：

| Darcy 分布 | CoCoGen 五任务宏平均相对 L2 | FM4PDE 五任务宏平均相对 L2 |
|---|---:|---:|
| ID | 33.37% | 14.02% |
| rough | 34.49% | 25.25% |

这两个分布中，CoCoGen 在完整观测正问题上的误差更低，在反问题、稀疏正问题和稀疏联合任务上更高。每个分布的五种任务复用其对应的同一批 1,000 个样本，不能把 5,000 次任务评估当成 5,000 个独立样本。Darcy smooth 尚待完成，不产生 Darcy 三分布总体或四模型总体宏平均，正式配置继续冻结。

rough 子组结果保存在 `docs/execution_20260915/darcy_rough_complete_comparison.json`，绑定独立保留的 `metrics_first60_12cells.json` 快照；该快照生成于 2026-09-15 16:04:34 CST，SHA256 为 `5f934227d697eeab04ea37f221b14a868d8cdc8f450cdd5aceb1f4870c5def1c`。服务器对应源文件为 `reports/snapshots/metrics_first60_12cells.json`。同一快照也更新到 `metrics_first60_partial.json` 与目标场 CSV，八单元快照及 ID 子组结果保持独立保留。

### 16:46 CST 更新：Poisson 稀疏正问题与 Darcy smooth 正问题

新增两个单元各完成 1,000 个正式样本，累计 14/60 个单元、16 个目标场比较、448 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Poisson ID，稀疏正问题 | u | 48.05% | 10.15% | 0.3% | 41.05 |
| Darcy smooth，完整观测正问题 | u | 0.70% | 5.69% | 99.8% | 41.89 |

Poisson 在仅提供输入场 a 的 500 个观测点时，解场平均误差约为 FM4PDE 的 4.73 倍。结合其已完成的完整观测正问题（6.20% 对 7.99%），当前配置对观测是否完整表现出明显差异；这两项任务沿用同一批正式样本和各自的历史观测设置，未根据正式结果重新调参。

Darcy 完整观测正问题的三种分布已全部完成：ID 为 0.70% 对 7.78%，smooth 为 0.70% 对 5.69%，rough 为 0.74% 对 12.46%。这支持当前配置在已知完整输入场时预测 Darcy 解较准确的结论。Darcy smooth 的其余四种任务、Poisson 的其余十二个单元和另两个 PDE 仍待完成，不将这一任务的优势推广为总体优势。

本次快照生成于 2026-09-15 16:46:00 CST，保存到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `0d581da9d794d876e62d3636e8d43ecd4f0a8cfc324cfa1b8f684f909c58fe74`。CSV 在本地归档中统一为 LF 换行，数值保持与服务器一致；历史快照通过 Git 保留。两个新增单元的耗时继续接近原估计，保留四模型含余量 22.15 小时的启动前预测。

### 17:28 CST 更新：Poisson 稀疏反问题与 Darcy smooth 完整观测反问题

新增两个单元各完成 1,000 个正式样本，累计 16/60 个单元、18 个目标场比较、512 个预测文件通过收集器核验。两项都以输入场 a 为评估目标。

| PDE、分布与任务 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---:|---:|---:|---:|
| Poisson ID，稀疏反问题 | 76.74% | 24.54% | 0.0% | 41.07 |
| Darcy smooth，完整观测反问题 | 46.84% | 10.55% | 0.0% | 41.95 |

Poisson 稀疏反问题的平均误差约为 FM4PDE 的 3.13 倍，Darcy smooth 完整观测反问题约为 4.44 倍；两个单元各自的 1,000 个样本中 CoCoGen 误差均更大。这些结果按正式冻结配置报告，没有依据正式样本重新调参。

Darcy 完整观测反问题的三种分布现已齐备：ID 为 49.16% 对 19.05%，smooth 为 46.84% 对 10.55%，rough 为 55.54% 对 38.26%。与完整观测正问题的三分布结果结合看，当前配置在已知完整输入场时预测解较准确，在已知完整解场时反推输入场较弱。Darcy smooth 的三种稀疏任务、Poisson 的其余十一单元及另两个 PDE 仍待完成，不产生未完成 PDE 或四模型总体宏平均。

本次快照生成于 2026-09-15 17:28:12 CST，保存到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV；JSON 的 SHA256 为 `671aa2816d022b048fe352d6fed7c95c17697ff402a7eac26b27006fd6222e1c`。历史快照通过 Git 保留，Darcy ID/rough 子组继续绑定原有独立快照。新增单元耗时仍接近启动前估计，两卡继续后续评估，Burgers 仍等待前 60 单元完成。

### 18:10 CST 更新：完整 Poisson ID 五任务与 Darcy smooth 稀疏正问题

新增两个单元各完成 1,000 个正式样本，累计 18/60 个单元、21 个目标场比较、576 个预测文件通过收集器核验。联合任务在下表按两个目标场分别列出。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---|---:|---:|---:|
| Poisson ID，稀疏联合重建 | a | 30.17% | 18.78% | 0.1% |
| Poisson ID，稀疏联合重建 | u | 9.27% | 5.67% | 6.2% |
| Darcy smooth，稀疏正问题 | u | 20.62% | 6.13% | 1.9% |

Poisson 联合任务的两场误差分别约为 FM4PDE 的 1.61 和 1.63 倍；Darcy smooth 稀疏正问题约为 3.36 倍。两个新增单元的纯采样耗时分别为 41.09 和 41.95 分钟，仍接近启动前估计。

Poisson ID 五种任务全部齐备，使用与 Darcy 子组相同的口径：先对联合任务两场等权平均，再对五种任务等权平均。

| Poisson ID 任务 | CoCoGen 相对 L2 | FM4PDE 相对 L2 |
|---|---:|---:|
| 完整观测正问题，u | 6.20% | 7.99% |
| 完整观测反问题，a | 30.42% | 24.55% |
| 稀疏正问题，u | 48.05% | 10.15% |
| 稀疏反问题，a | 76.74% | 24.54% |
| 稀疏联合重建，a/u 等权 | 19.72% | 12.22% |
| 五任务宏平均 | **36.23%** | **15.89%** |

当前配置在这个完整 ID 子组中，完整观测正问题误差更低，其余四种任务更高。五种任务复用同一批 1,000 个样本，不能将 5,000 次任务评估当成 5,000 个独立样本估计不确定性。Poisson smooth/rough、Darcy smooth 的稀疏反问题与联合任务，以及另外两个 PDE 仍待完成，不产生未完成 PDE 或四模型总体宏平均。正式采样参数继续冻结。

Poisson ID 子组结果保存在 `docs/execution_20260915/poisson_id_complete_comparison.json`，绑定独立保留的 `metrics_first60_18cells.json` 快照。快照生成于 2026-09-15 18:10:30 CST，SHA256 为 `c79afd20801ec8b3a385e704ba8ceb0405c372f1ab9266994a18a6bfab3c7636`，服务器源文件为 `reports/snapshots/metrics_first60_18cells.json`。同一快照更新到 `metrics_first60_partial.json` 与目标场 CSV，前两份 Darcy 子组快照保持独立保留。

### 18:56 CST 更新：Poisson rough 正问题与 Darcy smooth 稀疏反问题

新增两个单元各完成 1,000 个正式样本，累计 20/60 个单元、23 个目标场比较、640 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Poisson rough，完整观测正问题 | u | 8.85% | 7.95% | 35.9% | 41.16 |
| Darcy smooth，稀疏反问题 | a | 62.40% | 11.15% | 0.0% | 41.94 |

Poisson rough 完整观测正问题的平均误差约高 11.4%，其 ID 同任务的优势没有延续到 rough 分布；不能将 Darcy 完整观测正问题的三分布优势推广到 Poisson。Darcy smooth 稀疏反问题的平均误差约为 FM4PDE 的 5.59 倍，1,000 个样本中均更高。Darcy 仅剩 smooth 稀疏联合任务；当前尚不产生 Darcy 三分布总体或四模型总体宏平均，正式配置继续冻结。

快照生成于 2026-09-15 18:56:19 CST，保存到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV；JSON 的 SHA256 为 `0d29d393262b2b539f3625019d3442c5d5d4c1ad3fd6011970dae69d128521a8`。历史快照通过 Git 保留，三个完整分布子组继续绑定原有独立快照。

18:55:56 CST 的进度检查显示，连同进行中的批次已保存 20,352/60,000 次样本评估；GPU 0 的 Poisson rough 完整观测反问题完成 256/1,000，GPU 1 的 Darcy smooth 稀疏联合任务完成 96/1,000。两张卡利用率分别为 96% 和 100%，实际工作进程均存活，没有退出记录。按启动前模型吞吐和 15% 余量估计，四模型剩余约 12–15 小时，预计 9 月 16 日上午 7–10 点完成；此为预测，Burgers 训练与评估另计。

### 19:34 CST 更新：Darcy 全部 15 单元完成，Helmholtz 正常接续

新增 Poisson rough 完整观测反问题和 Darcy smooth 稀疏联合任务各完成 1,000 个正式样本。累计 22/60 个单元、26 个目标场比较、704 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---|---:|---:|---:|
| Poisson rough，完整观测反问题 | a | 49.45% | 49.29% | 49.4% |
| Darcy smooth，稀疏联合重建 | a | 51.92% | 19.28% | 0.0% |
| Darcy smooth，稀疏联合重建 | u | 16.57% | 2.96% | 0.0% |

Poisson 此项平均误差接近，CoCoGen 高约 0.16 个百分点；这一均值差不代表已证明统计显著性或方法等效。Darcy smooth 联合任务的系数场和解场误差分别约为 FM4PDE 的 2.69 和 5.59 倍，两个目标场的 1,000 个样本中 CoCoGen 误差均更高。

Darcy 三种分布、五种任务全部齐备。每个任务先对目标场等权平均，再对五任务及三分布等权汇总：

| Darcy 范围 | CoCoGen 宏平均相对 L2 | FM4PDE 宏平均相对 L2 |
|---|---:|---:|
| ID，五任务 | 33.37% | 14.02% |
| smooth，五任务 | 32.96% | 8.93% |
| rough，五任务 | 34.49% | 25.25% |
| 三分布全部 15 任务单元 | **33.61%** | **16.07%** |

CoCoGen 在三个完整观测正问题单元上误差更低，在其余十二个单元上更高。三分布总体平均误差约为 FM4PDE 的 2.09 倍，说明当前冻结配置的优势集中在已知完整系数场时预测解，不能推广到反问题或稀疏观测设置。15,000 次任务评估会在每个分布内复用样本编号，不能视为 15,000 个独立样本。正式参数未重新选择，Poisson、Helmholtz、NS 和四模型总体指标仍未齐备。

Darcy 累计纯采样耗时为 **10.4495 GPU 小时**，接近启动前 10.4583 GPU 小时的估计；这是两卡所执行 Darcy 采样时间之和，不能直接当成墙钟耗时。两个新增单元的纯采样时间分别为 41.16 分钟（Poisson）和 41.94 分钟（Darcy）。两卡继续按原分工运行，Burgers 的前置条件仍是四模型全部 60 单元完成核验。

本次快照生成于 2026-09-15 19:34:34 CST，独立保留为 `docs/execution_20260915/metrics_first60_22cells.json`，SHA256 为 `8c12733d48adb15c55aa66e17806b5a0a6457f29c9715a867fa68e63483ed0bb`；服务器对应 `reports/snapshots/metrics_first60_22cells.json`。完整 Darcy 报告为 `docs/execution_20260915/darcy_complete_comparison.json`，绑定该独立快照并保留每场、每任务和每分布结果。最新 partial JSON 与 CSV 同步更新，较早三个分布子组的独立快照保持不变。

GPU 0 已接续 Helmholtz。前两批共 64 例于 19:28:24 CST 通过 CPU 复核：预测文件哈希、正式编号与有效 mask、基础模型权重与冻结配置、采样实现均一致，观测值精确保留，保存的相对 L2 可在 CPU float64 中重新计算，实际为 500 NFE 且到达时间 0。两批分别耗时 55.66 和 55.68 秒，记录在 `docs/execution_20260915/validation_helmholtz_transition.json`。该检查证明接续后的前两批正确，不作为 Helmholtz 完整单元效果结论；同进程已预热情况下的峰值显存也不替代新进程启动预算。

### 19:55 CST 更新：首个完整 Helmholtz 单元

Helmholtz ID 完整观测正问题已完成 1,000 个正式样本。累计 23/60 个单元、27 个目标场比较、736 个预测文件通过收集器核验，样本编号、冻结配置、预测哈希与历史指标配对均通过。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz ID，完整观测正问题 | u | **2.89%** | 13.75% | 99.9% | 29.06 |

此项 CoCoGen 平均误差约为 FM4PDE 的 0.21 倍，即降低约 79%；1,000 个样本中有 999 个误差更低。评估仅统计未知解 u，不把已观测输入场 a 的零误差计入比较。Helmholtz 其余十四个单元尚未完成，因此该优势只适用于当前已验证的 ID 完整观测正问题，仍不产生 Helmholtz 总体或四模型总体宏平均。

纯采样耗时为 1,743.44 秒，接近启动前每单元约 1,736.05 秒的预测。沿用正式前冻结的基础模型、500 NFE 和物理修正配置；两卡继续后续单元。快照生成于 2026-09-15 19:55:53 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `590d15f02120d1ef615e600ef617090d46b8718c4c2bd72bf65d023a1c343074`。历史 partial 版本通过 Git 保留，完整 Darcy 报告继续绑定独立的 22 单元快照。

### 20:15 CST 更新：Poisson rough 稀疏正问题

Poisson rough 稀疏正问题已完成 1,000 个正式样本。累计 24/60 个单元、28 个目标场比较、768 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Poisson rough，稀疏正问题 | u | 50.01% | 12.60% | 0.4% | 41.52 |

CoCoGen 此项平均误差约为 FM4PDE 的 3.97 倍，仅 4/1,000 个样本误差更低。结合此前 ID 稀疏正问题的 48.05% 对 10.15%，当前配置在 Poisson 已完成的两个分布的稀疏正问题上均明显落后。Poisson 仍有七个单元未完成，尚不产生其三分布总体指标；采样参数继续保持冻结。

本次快照生成于 2026-09-15 20:15:55 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `cee8275df9bc1e2310cef5ce85d6872089a8a879686206420e939d826e845d35`。历史 partial 版本通过 Git 保留，完整 Darcy 报告继续绑定原有独立快照。新增单元的 41.52 分钟耗时仍接近启动前预算，四模型含余量 22.15 小时的预计总耗时保持不变。

### 20:24 CST 更新：Helmholtz 完整观测反问题

Helmholtz ID 完整观测反问题已完成 1,000 个正式样本。累计 25/60 个单元、29 个目标场比较、800 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz ID，完整观测反问题 | a | 31.22% | 23.87% | 2.6% | 29.03 |

CoCoGen 在已知完整解场、反推输入场的这一任务上，平均误差约高 30.8%，仅 26/1,000 个样本误差更低。与同一 ID 分布完整观测正问题的 2.89% 对 13.75% 对照，当前配置在正问题上的优势没有延续到反问题。Helmholtz 尚有十三个单元未完成，不产生其总体指标，也不根据正式结果重新选择参数。

本次快照生成于 2026-09-15 20:24:35 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `243c1624b1ad90ef4770f060510f8ea5dbb4df6c3524363dbaa63a6900d698bd`。历史 partial 版本通过 Git 保留，Darcy 完整报告继续绑定原有独立快照。该单元纯采样耗时 1,741.84 秒，仍接近启动前估计。

### 20:57 CST 更新：Helmholtz 稀疏正问题与 Poisson rough 稀疏反问题

新增两个单元各完成 1,000 个正式样本。累计 27/60 个单元、31 个目标场比较、864 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz ID，稀疏正问题 | u | 56.43% | 13.97% | 0.9% | 28.98 |
| Poisson rough，稀疏反问题 | a | 80.47% | 49.43% | 0.0% | 41.52 |

Helmholtz 稀疏正问题的平均误差约为 FM4PDE 的 4.04 倍，仅 9/1,000 个样本更低。结合其完整观测正问题的 2.89% 对 13.75%，当前已验证的优势没有延续到稀疏观测设置。Helmholtz 已完成的三个 ID 任务中，完整观测正问题更好，完整观测反问题和稀疏正问题更差；其余十二个单元仍待完成。

Poisson rough 稀疏反问题的平均误差约为 FM4PDE 的 1.63 倍，1,000 个样本中均更高。此前 rough 完整观测反问题为 49.45% 对 49.29%，在本次稀疏观测设置下，两者的平均误差差距增大。Poisson rough 尚缺联合任务，smooth 的五任务也未完成，不产生其总体宏平均。

本次快照生成于 2026-09-15 20:57:32 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `03da81d440ff72b674ee700f9d1b5c570c5c235e17acb3eca0c9259100cc3f7c`。历史版本通过 Git 保留，Darcy 完整报告继续绑定原有独立快照。两个新增单元耗时仍接近原估计，正式配置继续冻结，四模型预计完成时间保持在 9 月 16 日上午。

### 21:13 CST 实测剩余时间估计

此时实际采样进程仍为 PID 848262、848266，已运行 9.36 小时；27 个完整单元加两个在途单元共保存 28,056 次评估。已完成单元的平均纯采样耗时为 Darcy 41.80 分钟、Poisson 41.21 分钟、Helmholtz 29.02 分钟。

按冻结的两卡任务分配，对每个剩余单元使用“尚未保存的样本数 × 实测每样本耗时”估计：优先使用同 GPU、同 PDE 的完整单元，没有时使用该 PDE 两卡汇总；NS 尚无正式完整单元，继续使用启动前独立验证测速。两卡中较长的剩余计算路径约为 **10.00 小时**，对应 **9 月 16 日 07:13 CST**；仅对剩余时间增加 15% 余量后为 **08:43 CST**，预计启动后共 **20.86 小时**。这与原启动前全程加余量的 **10:01 CST、22.15 小时**预测口径不同，两者均仍落在用户约一天的目标内。

估计未逐项计入未来模型加载、I/O 和最终核验开销；正在计算但未保存的批次仍计入剩余样本，NS 速度还需正式实跑确认。Burgers 训练及其六个评估单元另计。详细分配、实测依据和源摘要/批次收据哈希保存在 `docs/execution_20260915/runtime_projection_27cells.json`。这份文件是时间估计，不能作为任何新增单元的完成或质量核验凭据。

### 21:22 CST 更新：Helmholtz ID 稀疏反问题完成

新增单元 `helmholtz/id/sparse_inverse` 已完成 1,000 个正式样本。累计 28/60 个单元、32 个目标场比较、896 个预测文件通过收集器核验。

| 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz ID，稀疏反问题 | a | 79.64% | 24.23% | 0.0% | 28.98 |

CoCoGen 的平均误差约为 FM4PDE 的 3.29 倍，1,000 个配对样本中均更高。此前完整观测反问题为 31.22% 对 23.87%，本次稀疏反问题的差距更大。Helmholtz 已完成的四个 ID 任务中，完整观测正问题领先，另外三个均落后；ID 联合任务及其他分布尚未完成，因此仍不产生 Helmholtz 总体宏平均。

本次快照生成于 2026-09-15 21:22:50 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `11f925595748d1ca093b39853b531a360551779e1a08b34036b2115cf66b3160`。该单元纯采样耗时 1,739.00 秒，保持在原预计速度附近。正式配置继续冻结，两个工作进程继续后续任务。

### 21:38 CST 更新：Poisson rough 五任务全部完成

新增单元 `poisson/rough/sparse_joint` 已完成 1,000 个正式样本。累计 29/60 个单元、34 个目标场比较、928 个预测文件通过收集器核验。

| Poisson rough 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---|---:|---:|---:|
| 完整观测正问题 | u | 8.85% | 7.95% | 35.9% |
| 完整观测反问题 | a | 49.45% | 49.29% | 49.4% |
| 稀疏正问题 | u | 50.01% | 12.60% | 0.4% |
| 稀疏反问题 | a | 80.47% | 49.43% | 0.0% |
| 稀疏联合重建 | a | 42.78% | 41.07% | 27.1% |
| 稀疏联合重建 | u | 10.25% | 8.24% | 19.4% |

联合任务先对 a/u 等权平均、再对五任务等权平均，Poisson rough 的宏平均相对 L2 为 **CoCoGen 43.06%、FM4PDE 28.78%**，前者约为后者的 **1.50 倍**。五个任务的平均误差都更高；稀疏正问题差距最大，约为 FM4PDE 的 3.97 倍。完整观测反问题的均值只相差约 0.16 个百分点，该结果不代表统计等价性检验通过。新增联合重建的 a/u 两个目标场均落后，但平均误差差距小于两个稀疏单向任务。

本次快照生成于 2026-09-15 21:38:49 CST，独立保留为 `docs/execution_20260915/metrics_first60_29cells.json`，SHA256 为 `130d76564373a30d3b208d6aab64f035dfcb5f6d5c43695f952f1cea42eef3ed`；服务器对应 `reports/snapshots/metrics_first60_29cells.json`。子组结果保存为 `docs/execution_20260915/poisson_rough_complete_comparison.json`，绑定该快照。五任务复用同一分布的 1,000 个样本，合计 5,000 次任务评估；Poisson smooth 和四模型整体仍未完成，因此仍不产生 Poisson 总体或四模型宏平均。新增联合任务纯采样 2,491.96 秒（41.53 分钟），正式配置保持冻结。

### 21:52 CST 更新：Helmholtz ID 五任务全部完成

新增单元 `helmholtz/id/sparse_joint` 已完成 1,000 个正式样本。累计 30/60 个单元、36 个目标场比较、960 个预测文件通过收集器核验。四模型的正式单元数达到一半，剩余 Poisson smooth 五任务、Helmholtz rough/smooth 十任务和 NS 十五任务。

| Helmholtz ID 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---|---:|---:|---:|
| 完整观测正问题 | u | 2.89% | 13.75% | 99.9% |
| 完整观测反问题 | a | 31.22% | 23.87% | 2.6% |
| 稀疏正问题 | u | 56.43% | 13.97% | 0.9% |
| 稀疏反问题 | a | 79.64% | 24.23% | 0.0% |
| 稀疏联合重建 | a | 30.03% | 22.85% | 5.1% |
| 稀疏联合重建 | u | 9.55% | 7.04% | 16.4% |

联合任务先对 a/u 等权平均、再对五任务等权平均，Helmholtz ID 的宏平均相对 L2 为 **CoCoGen 37.99%、FM4PDE 18.15%**，前者约为后者的 **2.09 倍**。完整观测正问题领先，另外四个任务的平均误差均更高。新增联合重建的输入场 a 误差约为 FM4PDE 的 1.31 倍，解场约为 1.36 倍；稀疏单向任务的差距更大。该结论只适用于 ID 五任务，Helmholtz 的另外两个分布尚未完成。

本次快照生成于 2026-09-15 21:52:06 CST，独立保留为 `docs/execution_20260915/metrics_first60_30cells.json`，SHA256 为 `7985e533f08e7fac07298074d0da32c7c6b9d65ed74798e24f7a3937648cd707`；服务器对应 `reports/snapshots/metrics_first60_30cells.json`。子组结果保存为 `docs/execution_20260915/helmholtz_id_complete_comparison.json`，绑定该快照。五任务复用 ID 分布的 1,000 个样本，合计 5,000 次任务评估；仍不产生 Helmholtz 总体或四模型宏平均。新增联合任务纯采样 1,741.19 秒（29.02 分钟），正式配置继续冻结。

### 22:20 CST 更新：Poisson smooth 与 Helmholtz rough 完整观测正问题

新增两个单元各完成 1,000 个正式样本。累计 32/60 个单元、38 个目标场比较、1,024 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz rough，完整观测正问题 | u | 8.14% | 14.32% | 91.4% | 28.99 |
| Poisson smooth，完整观测正问题 | u | 8.90% | 11.37% | 68.2% | 41.51 |

两项新增任务的 CoCoGen 平均误差均更低：Helmholtz rough 约降低 43.1%，914/1,000 个样本更优；Poisson smooth 约降低 21.8%，682/1,000 个样本更优。Poisson 的完整观测正问题现已覆盖三个分布：ID、smooth 领先，rough 落后。Helmholtz 的完整观测正问题在已完成的 ID、rough 两个分布上均领先，smooth 尚待完成。这些结果不能代替尚未完成的其他任务和 PDE 总体指标。

本次快照生成于 2026-09-15 22:20:53 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `bb4b56a0830d5fa5e80bf32dcf1e0e7b1f5077be89c702e32c00a1356c48a4e1`。历史版本通过 Git 保留，已有完整 PDE/分布子组继续绑定各自独立快照。两个新增单元纯采样分别为 1,739.17 秒和 2,490.36 秒，保持在原预计速度附近。正式配置继续冻结，两张 GPU 接续各自分布的完整观测反问题。

### 22:49 CST 更新：Helmholtz rough 完整观测反问题完成

新增单元 `helmholtz/rough/full_inverse` 已完成 1,000 个正式样本。累计 33/60 个单元、39 个目标场比较、1,056 个预测文件通过收集器核验。

| 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz rough，完整观测反问题 | a | 50.85% | 50.03% | 37.5% | 28.99 |

CoCoGen 的平均误差高约 **0.81 个百分点**，约为 FM4PDE 的 **1.016 倍**；375/1,000 个样本误差更低。两者均值较接近，但输入场 a 重建误差都在 50% 左右。此前 Helmholtz ID 完整观测反问题为 31.22% 对 23.87%，rough 上双方误差都更高，而两者的均值差距较小；这不代表统计等价性检验通过。Helmholtz rough 的正问题领先、反问题略落后，三个稀疏任务仍待完成。

本次快照生成于 2026-09-15 22:49:44 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `32df080978a658c9be88f1c5035b5a1a4a9b0777cc1e25cbba28b6fd0eb22a82`。该单元纯采样 1,739.22 秒，保持在原预计速度附近。正式配置继续冻结，两个采样进程接续后续任务。

### 23:01 CST 更新：Poisson smooth 完整观测反问题完成

新增单元 `poisson/smooth/full_inverse` 已完成 1,000 个正式样本。累计 34/60 个单元、40 个目标场比较、1,088 个预测文件通过收集器核验。

| 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Poisson smooth，完整观测反问题 | a | 34.92% | 24.16% | 1.1% | 41.51 |

CoCoGen 的平均误差高约 **10.76 个百分点**，约为 FM4PDE 的 **1.45 倍**；11/1,000 个样本误差更低。Poisson 的完整观测反问题已覆盖三个分布，三个分布的 CoCoGen 平均误差均更高，其中 rough 的均值差距最小、smooth 最大。Poisson 已完成 12/15 个单元，仍待 smooth 的稀疏正问题、稀疏反问题和联合重建完成后，才计算该 PDE 的总体宏平均。

本次快照生成于 2026-09-15 23:01:44 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `4793af886fa1c04a7a3dc65b0c26c5b825e8bbfdde4fee0a57e86d3f5f3f4bf2`。该单元纯采样 2,490.76 秒，保持在原预计速度附近。正式配置继续冻结，两张 GPU 接续各自分布的稀疏正问题。

### 23:18 CST 更新：Helmholtz rough 稀疏正问题完成

新增单元 `helmholtz/rough/sparse_forward` 已完成 1,000 个正式样本。累计 35/60 个单元、41 个目标场比较、1,120 个预测文件通过收集器核验。

| 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz rough，稀疏正问题 | u | 55.47% | 17.06% | 1.1% | 28.99 |

CoCoGen 的平均误差高约 **38.41 个百分点**，约为 FM4PDE 的 **3.25 倍**；11/1,000 个样本误差更低。同分布完整观测正问题此前为 8.14% 对 14.32%，其优势没有延续到稀疏输入场观测。Helmholtz 的 ID 和 rough 两个分布都呈现完整观测正问题领先、稀疏正问题落后的结果。rough 的稀疏反问题和联合重建尚待完成，仍不计算该分布的五任务宏平均或 Helmholtz 总体指标。

本次快照生成于 2026-09-15 23:18:46 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `85c5679e740743813dc6c224a7a8be2790f6b5dcd84ede59d2b25195bf1225d7`。该单元纯采样 1,739.35 秒，保持在原预计速度附近。正式配置继续冻结，Helmholtz 接续 rough 稀疏反问题，Poisson 继续 smooth 稀疏正问题。

### 23:48 CST 更新：Poisson smooth 稀疏正问题与 Helmholtz rough 稀疏反问题

新增两个单元各完成 1,000 个正式样本。累计 37/60 个单元、43 个目标场比较、1,184 个预测文件通过收集器核验。

| PDE、分布与任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Helmholtz rough，稀疏反问题 | a | 82.35% | 52.93% | 0.0% | 29.02 |
| Poisson smooth，稀疏正问题 | u | 53.50% | 13.78% | 1.2% | 41.51 |

Helmholtz rough 稀疏反问题的 CoCoGen 平均误差约为 FM4PDE 的 **1.56 倍**，高 **29.43 个百分点**，1,000 个样本中均更高。同分布完整观测反问题此前为 50.85% 对 50.03%，稀疏观测设置下的差距明显增大。该分布还差联合重建，尚不产生完整五任务宏平均。

Poisson smooth 稀疏正问题的 CoCoGen 平均误差约为 FM4PDE 的 **3.88 倍**，高 **39.72 个百分点**，12/1,000 个样本误差更低。Poisson 的稀疏正问题现已覆盖 ID、rough、smooth 三个分布，三个分布均落后。Poisson 剩余 smooth 稀疏反问题和联合重建，仍不产生该 PDE 的总体宏平均。

本次快照生成于 2026-09-15 23:48:15 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `fd48d87f831591f44654fe852008f8d0ed6cba871a528bc7742098c7675fce93`。历史版本通过 Git 保留，已有完整 PDE/分布子组继续绑定各自独立快照。新增两个单元纯采样分别为 1,741.10 秒和 2,490.46 秒，保持在原预计速度附近。正式配置继续冻结，Helmholtz 接续 rough 联合重建，Poisson 接续 smooth 稀疏反问题。

### 9 月 16 日 00:17 CST 更新：Helmholtz rough 五任务全部完成

新增单元 `helmholtz/rough/sparse_joint` 已完成 1,000 个正式样本。累计 38/60 个单元、45 个目标场比较、1,216 个预测文件通过收集器核验。Helmholtz 已完成 ID、rough 的十个单元，尚缺 smooth 五任务。

| Helmholtz rough 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---|---:|---:|---:|
| 完整观测正问题 | u | 8.14% | 14.32% | 91.4% |
| 完整观测反问题 | a | 50.85% | 50.03% | 37.5% |
| 稀疏正问题 | u | 55.47% | 17.06% | 1.1% |
| 稀疏反问题 | a | 82.35% | 52.93% | 0.0% |
| 稀疏联合重建 | a | 43.07% | 45.60% | 79.7% |
| 稀疏联合重建 | u | 10.41% | 9.23% | 30.7% |

联合任务先对 a/u 等权平均、再对五任务等权平均，Helmholtz rough 的宏平均相对 L2 为 **CoCoGen 44.71%、FM4PDE 32.35%**，前者约为后者的 **1.38 倍**。完整观测正问题和联合任务的平均误差更低，另外三个任务更高。

新增联合任务的两场均值为 **26.74% 对 27.41%**，CoCoGen 低约 **0.67 个百分点**。其输入场 a 误差低约 2.52 个百分点，797/1,000 个样本更优；解场误差高约 1.18 个百分点，仅 307/1,000 个样本更优。因此这个联合任务呈现输入场重建更好、解场重建更差的结果；小幅的两场平均优势来自输入场 a 的改善。该结论为本次固定种子结果，尚无多种子不确定性估计。

本次快照生成于 2026-09-16 00:17:16 CST，独立保留为 `docs/execution_20260915/metrics_first60_38cells.json`，SHA256 为 `27bc774fed0c1fcb1a7eb57c7c3038f58fbfe18b906a28679224aa9b54028018`；服务器对应 `reports/snapshots/metrics_first60_38cells.json`。子组结果保存为 `docs/execution_20260915/helmholtz_rough_complete_comparison.json`，绑定该快照。五任务复用 rough 分布的 1,000 个样本，合计 5,000 次任务评估；Helmholtz smooth 和其他剩余任务尚未完成，因此仍不产生 Helmholtz 总体或四模型宏平均。新增联合任务纯采样 1,738.47 秒（28.97 分钟），正式配置继续冻结。

### 9 月 16 日 00:25 CST 更新：Poisson smooth 稀疏反问题完成

新增单元 `poisson/smooth/sparse_inverse` 已完成 1,000 个正式样本。累计 39/60 个单元、46 个目标场比较、1,248 个预测文件通过收集器核验。

| 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 | CoCoGen 纯采样分钟 |
|---|---|---:|---:|---:|---:|
| Poisson smooth，稀疏反问题 | a | 88.92% | 24.13% | 0.0% | 41.51 |

CoCoGen 的平均误差高约 **64.79 个百分点**，约为 FM4PDE 的 **3.68 倍**；1,000 个样本中均更高。同分布完整观测反问题此前为 34.92% 对 24.16%，稀疏观测设置下的差距更大。Poisson 的稀疏反问题现已覆盖 ID、rough、smooth 三个分布，三个分布均落后。Poisson 已完成 14/15 个单元，只剩 smooth 联合重建，仍待该任务完成后计算总体宏平均。

本次快照生成于 2026-09-16 00:25:26 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，JSON 的 SHA256 为 `543a1d9d8150d4ba0d5400bb053098418f5216f8e9fc8bbea75284ee1d5f4502`。该单元纯采样 2,490.68 秒，保持在原预计速度附近。正式配置继续冻结，Poisson 接续 smooth 联合重建，Helmholtz 继续 smooth 完整观测正问题。

### 9 月 16 日 00:46 CST 更新：Helmholtz 完整观测正问题覆盖全部三分布

新增单元 `helmholtz/smooth/full_forward` 已完成 1,000 个正式样本。累计 40/60 个单元、47 个目标场比较、1,280 个预测文件通过收集器核验。

| Helmholtz 完整观测正问题 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更小的样本比例 |
|---|---:|---:|---:|
| ID | 2.89% | 13.75% | 99.9% |
| smooth | 3.83% | 18.60% | 99.6% |
| rough | 8.14% | 14.32% | 91.4% |

新增 smooth 单元中 CoCoGen 的平均误差低约 **14.77 个百分点**，相对降低约 **79.42%**，996/1,000 个样本误差更低。至此，Helmholtz 在已知完整输入场、预测解场的任务上，三个分布均有优势。该结论仅覆盖完整观测正问题；Helmholtz smooth 的其余四任务仍未完成，不产生该 PDE 总体或四模型总体宏平均。

本次快照生成于 2026-09-16 00:46:14 CST，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及目标场 CSV，并独立保留为 `metrics_first60_40cells.json`。JSON 的 SHA256 为 `454b660f41509ad7af7babd45a108ce57d30bf3b7f09e6d52e4ef97a49d38c72`。新增单元纯采样 1,740.80 秒（29.01 分钟），正式配置继续冻结。

00:47:35 CST 再次核实两个实际评估进程存活，累计保存 40,608 次评估。按各卡已完成同 PDE 单元的实测速度估算剩余样本，预计还需 **6.43 小时**纯计算，约 **07:13** 完成前 60 单元；对剩余时间添加 15% 余量后约 **08:11**。尚未开始正式采样的 NS 使用冻结独立验证的速度，未来模型加载、I/O 和最终核验耗时在加余量前没有单独计入。此估算不包含 Burgers 的训练、校准和六单元评估，也不是完成收据。详细分卡计算保存为 `docs/execution_20260915/runtime_projection_40cells.json`，绑定上述独立快照和冻结分工。

### 9 月 16 日 01:07 CST 更新：Poisson 全部十五单元完成

新增 `poisson/smooth/sparse_joint` 完成 1,000 个正式样本，累计 41/60 个单元、49 个目标场比较、1,312 个预测文件通过收集器核验。Poisson 与 Darcy 均具备完整 PDE 宏平均。

| Poisson 分布 | 五任务 CoCoGen 宏平均 | 五任务 FM4PDE 宏平均 | CoCoGen 误差更低的单元数 |
|---|---:|---:|---:|
| ID | 36.23% | 15.89% | 1/5 |
| smooth | 41.72% | 17.58% | 1/5 |
| rough | 43.06% | 28.78% | 0/5 |
| 三分布总体 | **40.34%** | **20.75%** | **2/15** |

Poisson 总体平均误差约为 FM4PDE 的 **1.94 倍**。ID、smooth 完整观测正问题领先，其余十三个单元均落后；全部稀疏任务均落后。新增 smooth 联合任务的 a/u 误差分别为 **33.99% / 10.78%**，对应 FM4PDE 为 **20.37% / 8.60%**；两目标等权均值为 22.38% 对 14.48%。该 Poisson 的 a 表示源项输入场，对应 `laplace(u)=a`。

新增单元纯采样 2,488.54 秒（41.48 分钟），Poisson 全部十五单元累计 **10.3323 GPU 小时**，每单元 41.05–41.53 分钟。GPU 1 已接续 `helmholtz/smooth/sparse_joint`，首批 32 例落盘、耗时 56.24 秒；两张卡实际 worker 进程均存活。Burgers 仍等待完整前 60 单元，不因 Poisson 完成而启动。

完整 Poisson 比较保存在 `docs/execution_20260915/poisson_complete_comparison.json`，并绑定独立的 `metrics_first60_41cells.json` 快照（生成时间 2026-09-16 01:07:02 CST，SHA256 为 `858479a1b786215bcfd2e92dde860d227f9cf01c48eefb5d3d43adf24540a228`）。服务器分别对应 `reports/poisson_complete_comparison.json` 与 `reports/snapshots/metrics_first60_41cells.json`。完整解读见 [Poisson 完整评估总结](poisson_complete_results_20260916.md)。本次继续使用正式前冻结的配置，尚不产生 Helmholtz、NS 或四模型总体宏平均。

### 9 月 16 日 01:23 CST 更新：Helmholtz smooth 完整反问题完成

新增 `helmholtz/smooth/full_inverse` 完成 1,000 个正式样本，累计 42/60 个单元、50 个目标场比较、1,344 个预测文件通过收集器核验。

| 任务 | 目标场 | CoCoGen 相对 L2 | FM4PDE 相对 L2 | CoCoGen 误差更低的样本比例 |
|---|---|---:|---:|---:|
| Helmholtz smooth，完整观测反问题 | a（源项输入场） | 36.53% | 23.26% | 0.2% |

该单元平均误差高约 **13.27 个百分点**，约为 FM4PDE 的 **1.57 倍**。Helmholtz 的完整观测反问题现已覆盖三个分布，三个均落后；完整观测正问题则三个均领先。不同任务方向的差异不能合并成该 PDE 的总体优势，剩余三个 smooth 稀疏任务还未完成。

新增单元纯采样 1,744.68 秒（29.08 分钟）。本次快照生成于 2026-09-16 01:23:18 CST，JSON SHA256 为 `451b191f617945535a3b324c9a97585dc7b822b548c0d18708150ba336093dc3`，更新到 `docs/execution_20260915/metrics_first60_partial.json` 及对应 CSV，历史版本由 Git 保留。Darcy 和 Poisson 的完整比较继续绑定各自独立快照，正式配置保持冻结。

### 四十四个完整单元与 NS 首两批独立核验

2026-09-16 01:45 CST，收集器核验 44 个完整单元、53 个目标场和 1,408 份预测文件哈希。Darcy/Poisson 各 15 个、Helmholtz 14 个，NS 暂无完整单元。

| 新增 Helmholtz smooth 任务 / 目标场 | CoCoGen | FM4PDE | 逐例 CoCoGen 误差更小比例 |
|---|---:|---:|---:|
| 稀疏正问题，u | 67.41% | 17.37% | 1.3% |
| 稀疏联合重建，源项 a | 35.59% | 24.57% | 2.2% |
| 稀疏联合重建，u | 11.54% | 9.37% | 25.9% |

联合任务两场等权误差为 23.56% 对 16.97%。正问题和联合任务纯采样分别约 28.96、29.36 分钟。Helmholtz 尚缺 smooth 稀疏反问题，不产生完整 PDE 宏平均。

NS 实际首个单元为 `nsnonbounded/rough/sparse_forward`。使用 Git 固定的 `validate_saved_batches.py` 在 CPU 重载前两批共 64 例，核验历史输入/有效掩码、模型和采样配置标识、预测哈希、观测精确一致、独立 float64 相对 L2、500 NFE 和终点时间 0，全部通过。每例仅观测初始场 a 的 500 点，u 无直接观测；两个批次采样分别为 80.973 和 81.019 秒，分配峰值均为 5,091,132,416 字节。本检查没有重新计算 PDE 残差，也不是完整 1,000 例成绩。证据为 `docs/execution_20260915/validation_nsnonbounded_transition.json`，主归档为 `validation/nsnonbounded_transition.json`。

最新快照 SHA256 为 `6796073d7a4c50534564873d6bd2f378904d23ab78b62ca3aeede9f809e42aab`，生成于 2026-09-16 01:45:03 CST；本地 CSV 已与全部 53 个目标场逐项核验。已完成 Darcy/Poisson 的独立完整报告保持各自原始快照绑定。

### NS 正式实测速度与四十四单元时间更新

2026-09-16 02:01:44 CST，44 个完整单元加当前单元的已保存批次共 **45,216 次样本评估**。NS 首个 `rough/sparse_forward` 单元的 19 批、608 例均已保存，平均 **2.53165 秒/例**，即约 **81.013 秒/32 例**。其两张卡的未来 NS 耗时估算均采用这组实际速度：GPU 1 为同卡数据，GPU 0 尚未进入 NS，暂借用另一张相同型号卡的结果。其他 PDE 使用正式已完成单元的实测速度。

按每卡剩余未保存样本分别计算，最长一路仍需 **5.198 小时纯采样**，预计 **07:13:38 CST** 结束；只对当时剩余部分添加 15% 余量后为 **08:00:25 CST**。未保存的在途批次按全额剩余计入；切换、I/O、最终核验的新增开销在余量之前未单独建模。NS 其他任务/分布和 GPU 0 的速度仍可能不同，不能把单个单元当作全部 NS 任务的确定耗时。

Burgers 的训练、校准和六单元评估不包含在以上时间中，其真实训练测速仍待前 60 单元全部完成后进行。

证据为 `docs/execution_20260915/runtime_projection_44cells.json`，包含实际批次的收据路径、SHA256、样本数和耗时，以及每卡剩余单元、采用速度和计算公式；绑定 `metrics_first60_44cells.json` 独立快照。已独立重新核算两卡分工覆盖、所有速度来源、剩余样本数、总耗时与结束时间。此更新取代之前 NS 尚使用校准速度的当前预测，保留旧预测作历史记录。

### 四十五个完整单元与 Helmholtz 全部完成

2026-09-16 02:12:54 CST，收集器核验 45 个完整单元、54 个目标场和 1,440 份预测文件哈希。Darcy、Poisson、Helmholtz 均已完成全部 15 单元；NS 尚无完整单元。

Helmholtz 最后完成的 smooth 稀疏反问题为 **CoCoGen 94.35%、FM4PDE 23.29%**，对应 1,000 例中 CoCoGen 误差更低的比例为 0。其 15 单元宏平均为 **42.61% 对 23.47%**，误差比为 1.816。ID、smooth、rough 各自五任务平均分别为 37.99% / 18.15%、45.14% / 19.90%、44.71% / 32.35%。

CoCoGen 共有四个单元平均误差更低：三个完整观测正问题，以及 rough 稀疏联合任务；后者仅源项 a 更好，解场 u 更差。全部 18 个目标场结果与逐样本胜率详见 [Helmholtz 完整报告](helmholtz_complete_results_20260916.md)。累计采样 7.2565 GPU 小时，每单元 28.96–29.36 分钟。

完整比较保存在 `docs/execution_20260915/helmholtz_complete_comparison.json`，绑定独立的 `metrics_first60_45cells.json`，源快照 SHA256 为 `2bc690db1b26614cdc39ffcb30b7a4ec926996e5c3b63cba657a1802fd13e245`。已从 18 个目标场独立重算联合任务权重、各分布宏平均、总体宏平均、胜出单元数、采样耗时，并核对报告全部表格值。

02:15 CST 实际日志确认 GPU 0 已进入 `nsnonbounded/id/full_forward`，完成前 64 例，两批耗时 80.081、80.084 秒；GPU 1 同时继续 `nsnonbounded/rough/sparse_forward`。两卡实际 NS 速度接近，Burgers 等待进程仍在运行。

### 四十六个完整单元与首个 NS 正式成绩

2026-09-16 02:18:23 CST，首个 NS 单元 `nsnonbounded/rough/sparse_forward` 的全部 1,000 例通过完整收集核验，正式累计 **46/60 个单元、55 个目标场、1,472 个预测文件哈希**。

该任务仅向模型提供初始场 a 的 500 点观测，评价目标为 u。完整单元的物理相对 L2 为 **CoCoGen 56.25%、FM4PDE 19.50%**，差距 36.75 个百分点、误差比 2.884；逐例 CoCoGen 更低的比例为 0。这里只能判断 NS 的 rough 稀疏正问题，不能据此代替全部十五个 NS 单元的成绩。

该单元纯采样耗时 **2,535.383 秒，约 42.26 分钟**，与前面根据实际批次速度估算的约 42.2 分钟接近。GPU 0 同时执行 NS ID 完整观测正问题，GPU 1 接续剩余分配任务；Burgers 继续等待完整前 60 单元。

保留的独立快照为 `docs/execution_20260915/metrics_first60_46cells.json`，SHA256 为 `b750e6066d1bd11024936e053ac7a595fda291524c62b9a423a9af5f999b0b3f`。最新局部 JSON 与 CSV 的全部 55 个目标场已逐项核验。Helmholtz 完整报告仍绑定其原始 45 单元快照。

### NS 三个完整单元与图像对照（2026-09-16 03:09 CST）

03:02:22 的收集快照包含 **48/60 个单元、57 个目标场、1,536 个预测文件哈希**。新增 ID 全观测正问题 u 为 CoCoGen / FM4PDE **2.58% / 9.71%**，采样 2,505.34 秒；rough 稀疏反问题 a 为 **76.65% / 40.91%**，采样 2,535.85 秒。二者分别有 100% / 0% 的配对样本优于 FM4PDE。任务与分布不同，不能从这两项比较单独归因于观测稀疏程度。

固定快照为 `metrics_first60_48cells.json`，SHA256 为 `a1ad0df8a5d3c0fe25533b51ffd6c93ccf3c1e52166057f84f1d387b94df9ce9`。最新 CSV 的 57 个目标场与 JSON 逐项一致；NS 与总体宏平均仍为空。

新增两个 NS 单元各自通过只读取归档副本的 CPU 审计，逐例 NumPy float64 误差、精确观测、来源哈希均通过。与既有 46 单元审计取无重复并集，覆盖全部 48,000 次完整单元预测；联合覆盖记录为 `validation_archive_reader_48cells_coverage.json`。这不等于完成全 66 单元恢复或训练验证。

[正式同样本对照图](main_example_comparisons_20260916.md) 包含 16 页，绑定此前的 47 单元快照：Darcy / Poisson / Helmholtz 各五种 ID 任务，NS 仅 ID 全观测正问题。按编号使用第一例，原始物理量、同一 PDE 同一字段共享色标。六张 PNG 已实际目视检查；图内误差属于单例，不是 1,000 例均值。

### 五十个完整单元（2026-09-16 03:43 CST）

03:36:45 的第 49 单元快照与 03:43:09 的第 50 单元快照均保留。正式累计 **50/60 个单元、60 个目标场比较、1,600 个预测文件**；最新 CSV 与 JSON 的全部目标场逐项一致。两个新增单元的 1,000 例相对 L2 如下。

| NS 单元 | 目标场 | CoCoGen | FM4PDE | CoCoGen 逐例更低的比例 |
|---|---|---:|---:|---:|
| ID 全观测反问题 | 初始涡量 a | 12.07% | 11.91% | 42.1% |
| rough 稀疏联合 | 初始涡量 a | 50.33% | 43.76% | 0% |
| rough 稀疏联合 | 终态涡量 u | 30.42% | 14.08% | 0% |

ID 全观测反问题两者较接近，CoCoGen 高 0.155 个百分点，采样耗时 2,504.00 秒（41.73 分钟）。rough 联合任务的两场等权误差为 **40.38% / 28.92%**，耗时 2,536.26 秒（42.27 分钟）；这一任务的两个目标场均落后，不能用仅满足观测点代替未观测区域的重建质量。

两个新增单元均通过只读取归档输入和预测的独立 NumPy float64 误差重算，有效观测最大绝对误差为 0。与此前审计无重复合并，覆盖全部 50,000 次完整单元预测，并将 1,600 个预测文件哈希逐项绑定到最新 50 单元快照；联合覆盖记录为 `validation_archive_reader_50cells_coverage.json`。尚未完成的 NS 单元与 Burgers 不在此核验范围。

快照 `metrics_first60_49cells.json` 的 SHA256 为 `5a6a52e1c40938872efad44f4a8e411eaac4ca9d1142d5db526c87ddd2781fd3`；`metrics_first60_50cells.json` 为 `010b885a5b84f89ba1e1ff4e0c8dbbaa949ddc21ae699ecf6cea1ee633f83b39`。NS 尚未凑齐全部十五单元，不报告 NS 宏平均，也不报告四模型总体宏平均。采样配置和 Burgers 的先后顺序保持冻结。

### 第五十一个完整单元与首次自动发布（2026-09-16 04:18 CST）

新增 `nsnonbounded/id/sparse_forward` 的全部 1,000 例已完成，目标场 u 的物理相对 L2 为 **CoCoGen 56.57%、FM4PDE 11.43%**，相差 45.14 个百分点、误差比 4.949；逐例 CoCoGen 更低的比例为 0。采样耗时 **2,505.19 秒，41.75 分钟**。该结论只适用于当前权重和冻结的 500 NFE 配置，不是对所有 CoCoGen 训练和采样方法的结论。

这是自动收集器首次处理新完成单元。记录显示它已完成新快照生成、该单元 1,000 例的独立 NumPy float64 重算、精确观测核查、旧审计合并和公开 JSON/CSV 发布。随后在本地重新核对六份来源审计与发布记录，覆盖 **51 个单元、61 个目标场、1,632 个预测文件哈希关联**，新增与历史审计没有重复或遗漏。

本地快照为 `docs/execution_20260915/metrics_first60_51cells.json`，SHA256 为 `ab7f146e7920fe968b9d8055be3da7f3431774e13cd49839bc32f8d272f67482`。自动流程原始文件在 `reports/auto_first60_20260916/`，首次新增处理验证为 `validation_report_watch_first_increment.json`。最终 60 单元的汇总完成路径尚待实际执行；NS 与四模型总体宏平均仍为空。

### 第五十二个完整单元（2026-09-16 04:25 CST）

`nsnonbounded/smooth/full_forward` 的全部 1,000 例已完成并自动复核，目标场 u 的物理相对 L2 为 **CoCoGen 2.10%、FM4PDE 10.77%**，CoCoGen 低 8.67 个百分点、相对降低 80.51%；逐例误差更低的比例为 100%。纯采样耗时 **2,534.64 秒，42.24 分钟**。NS 目前两个已完成的全观测正问题均较好，但这不能代替尚未完成的十五单元整体评估。

新增单元的独立 NumPy float64 逐例误差与保存指标一致，全部有效观测逐值满足。自动收集与发布成功后，本地再次核对七份来源审计，覆盖 **52 个完整单元、62 个目标场、1,664 个预测文件哈希关联**；公开 CSV、指标快照和合并审计的对应关系一致。

快照为 `docs/execution_20260915/metrics_first60_52cells.json`，SHA256 为 `8278ed45cc27e15ae7a510ef4d1b0fc3d06ddc3f2b1b157bb925e46664217251`。自动流程的不可覆盖原始文件保存在 `reports/auto_first60_20260916/`，本地核验记录为 `validation_report_watch_52cells.json`。剩余八个 NS 单元继续运行；NS 与四模型总体宏平均仍为空。

### 第五十三个完整单元（2026-09-16 05:00 CST）

04:59:53 的自动快照新增 `nsnonbounded/id/sparse_inverse`，共 1,000 例。目标为初始涡量 a，物理相对 L2 为 **CoCoGen 74.83%、FM4PDE 12.68%**，相差 62.15 个百分点、误差比 5.901；CoCoGen 逐例更低的比例为 0。纯采样耗时 **2,505.57 秒，41.76 分钟**。终态场的稀疏观测逐值满足，仍不能保证初始场重建准确。

新增单元通过独立 NumPy float64 逐例重算及 32 个预测文件的哈希核查。本地增量复核确认，此前 52 单元的全部目标场行、单元汇总行和来源哈希与已验证快照一致，原审计引用保持不变；新增审计完整覆盖这一单元，没有重复或遗漏。因此当前合并覆盖为 **53 个完整单元、63 个目标场、1,696 个预测文件哈希关联**。

快照为 `docs/execution_20260915/metrics_first60_53cells.json`，SHA256 为 `78e0397340f4a8351eddc04fbe64f9cf976caa5721c73de8154c8783d8f9d493`；增量核验记录为 `validation_report_watch_53cells.json`。CSV 从同一快照重建后，文件哈希与服务器自动发布记录完全一致。NS 和四模型总体宏平均仍为空，后续七个 NS 单元继续按冻结配置运行。

### 第五十四个完整单元（2026-09-16 05:07 CST）

`nsnonbounded/smooth/full_inverse` 的全部 1,000 例已完成并自动复核。初始涡量 a 的物理相对 L2 为 **CoCoGen 11.73%、FM4PDE 10.77%**，相差约 0.96 个百分点、误差比 1.089；CoCoGen 逐例更低的比例为 5.8%。纯采样耗时 **2,534.74 秒，42.25 分钟**。

新增单元的独立 NumPy float64 逐例误差重算通过，完整终态观测逐值满足。增量核验同时确认此前 53 单元的字段指标、汇总行和全部来源哈希保持一致；合并审计覆盖 **54 个完整单元、64 个目标场、1,728 个预测文件哈希关联**。

快照为 `docs/execution_20260915/metrics_first60_54cells.json`，SHA256 为 `ecbbf737fe45ceca0876331d34e1b671e60e8d89662f4455c7d4b0c61d3e86a7`；记录为 `validation_report_watch_54cells.json`。CSV 从同一不可变快照重建，其哈希与服务器发布记录一致。NS 尚有六个单元未完成，因此不生成 NS 或四模型总体宏平均。

### 第五十五个完整单元（2026-09-16 05:41 CST）

`nsnonbounded/id/sparse_joint` 的全部 1,000 例已完成并自动复核。初始涡量 a 的相对 L2 为 **CoCoGen 39.00%、FM4PDE 17.89%**；末态场 u 为 **28.44%、8.24%**。两场等权误差为 **33.72%、13.06%**，两个目标场各自均没有样本取得更低误差。纯采样耗时 **2,503.97 秒，41.73 分钟**。

有效观测点逐值满足，独立 NumPy float64 逐例误差重算通过，新增 32 个预测文件哈希一致。增量复核同时确认此前 54 单元的字段、单元汇总与来源哈希保持不变。合并覆盖为 **55 个完整单元、66 个目标场、1,760 个预测文件哈希关联**；公开 CSV 可从同一快照逐字节重建。

不可变快照为 `docs/execution_20260915/metrics_first60_55cells.json`，SHA256 为 `b245f1f016646fd8443d9bcc70f0068a843671efa2c2e9340d4c6a70b53a114d`；记录为 `validation_report_watch_55cells.json`。NS 的 ID 五任务已齐备，但 rough 与 smooth 尚有五个单元未完成，因此仍不生成完整 NS 或四模型总体宏平均。

### 独立验证与正式 ID 结果对照

Darcy、Poisson、Helmholtz、NS 的 ID 五任务均已完成，可与正式启动前冻结配置的独立验证结果对照。独立验证每任务 64 个样本，编号 1032–1095；正式 ID 每任务 1,000 个样本，前三个 PDE 为 0–999，NS 为 2000–2999。两组都先对联合任务的两个目标场等权平均，再对五任务等权平均。

| PDE | CoCoGen 独立验证宏平均 | CoCoGen 正式 ID 宏平均 | 正式减验证，百分点 |
|---|---:|---:|---:|
| Darcy | 34.78% | 33.37% | -1.41 |
| Poisson | 36.25% | 36.23% | -0.03 |
| Helmholtz | 37.20% | 37.99% | +0.80 |
| NS | 36.29% | 35.95% | -0.33 |

四个 PDE 的 ID 整体均值没有出现从较低误差到较高误差的明显跳变；稀疏观测和反问题的较高误差已在独立验证中出现。例如 Poisson 稀疏反问题为验证 75.49%、正式 ID 76.74%，Helmholtz 为 77.38%、79.64%，NS 为 75.56%、74.83%。NS 的五任务均值差绝对值均低于 0.74 个百分点，其中稀疏正问题为 57.18% / 56.57%，稀疏联合为 34.18% / 33.72%。因此正式阶段的较高误差延续了验证阶段的表现，不能解释为仅在大批量正式评估时突然出现的退化。

此对照为不同样本上的描述性比较，不是配对检验或统计等价性结论，也不能排除两阶段共享的模型或实现问题。每组五任务复用样本编号，不能把任务次数当成独立样本数。它只覆盖四个已完成的 ID 子组，不覆盖 rough/smooth 或四模型全分布总体；不能单独确定权重、条件生成方式或采样预算的因果贡献。正式配置保持冻结。

最新结果保存在 `docs/execution_20260915/calibration_to_formal_id_first4.json`，绑定已审计的 `metrics_first60_55cells.json` 快照。四份原始验证收据中的每任务 64 个目标误差均已重新求均值，核对了确切样本编号、模型/配置/实现标识、每 PDE 十个批次的 NFE/终点时间；服务器上四份选定配置、四份验证收据、四份预测文件和指标快照共 13 个文件的 SHA256 均一致。本次比较不重新计算预测场的逐例误差或 PDE 残差。原三 PDE 对照 `calibration_to_formal_id_first3.json` 及其 30 单元快照仍独立保留。

### 完整主实验完成条件

`protocol/selected/<pde>.json` 保存选定配置及独立验证结果。正式预测按 `main/<pde>/<dist>/<setting>/batches/<offset>/` 分批保存，每批有预测、配置、输入与模型哈希、采样收据和误差；支持校验后恢复。

每个 PDE 必须完成 15×1,000 个样本后才产生 `main/<pde>/complete.json`。汇总器核验全部 60 单元和预测文件哈希后，写出 `reports/first60.csv` 与 `first60_complete.json`。这才满足启动 Burgers 补训的前置条件。任务已启动不等于评估已完成。

### 目标场指标收集

`cocogen_eval/collect_metrics.py` 仅使用 Python 标准库读取已保存结果，不加载模型或使用 GPU。正问题只统计 u，反问题只统计 a，联合任务分别保留 a/u，Burgers 只统计 u。逐例相对 L2 与主实验一致，计算区域为完整目标场，包含稀疏观测位置。

每个单元内先对目标场等权平均，再对一个完整 PDE 的所有单元等权平均；整体指标对 PDE 等权平均，避免 Burgers 的 6 个单元因数量较少而被赋予更低 PDE 权重。逐场原始均值、误差比、配对均值差和逐例胜率同时保留，不能只依据整体平均判断所有任务的效果。这里只使用一个冻结采样种子，未将其解释为多种子稳定性结论。

收集器重新核对冻结参考记录、样本编号、配置/checkpoint/实现哈希、每个预测文件哈希、实际 NFE，以及逐批误差和摘要/历史参考的配对汇总。完整模式还要求 60/66 个单元及汇总完成收据、CSV 哈希全部匹配。`--allow-partial` 允许收集已完成的完整单元，输出文件名和状态标明 partial；未完成的 PDE 和整体均不产生宏平均。

```bash
python -m cocogen_eval.collect_metrics --study /research_data/users/zhangxifeng/C01Python/FM4PDE/outputs/cocogen_main_20260915
python -m cocogen_eval.collect_metrics --study /research_data/users/zhangxifeng/C01Python/FM4PDE/outputs/cocogen_main_20260915 --include-burger
```

2026-09-15 12:41 CST，真实两个 Darcy 完整单元的首次收集检查已通过：恰好产生三个目标场比较，不包含 full_forward 的已观测 a；宏平均为空，默认完整模式拒绝其余 58 个单元缺失。后续在新增完整单元后更新收集，截至 9 月 16 日 05:41 CST 共五十五个完整单元、六十六个目标场比较；Darcy、Poisson、Helmholtz 已具备完整 PDE 宏平均，仍不产生其余未完成 PDE 或四模型总体宏平均。Darcy、Poisson、Helmholtz 的 ID/rough 五任务比较均保留为独立声明范围的子组。完整 60/66 模式仍待实际结果齐备后验证。服务器独立脚本由 Git 标签 `cocogen-metrics-collector-20260915-v1` 提取到 `source/collect_metrics.py`，运行中的采样检出保持冻结；局部快照写入 `reports/metrics_first60_partial.json`，最新快照同步到 `docs/execution_20260915/metrics_first60_partial.json`。

## 固定依赖归档与代码恢复检查

2026-09-15 19:07:40 CST，`cocogen_archive_dependencies` 退出码为 0。独立检出 `code_archive`（`2efdaa134007b302217713387b393dfb21942ac6`）中的 `cocogen_eval/archive_dependencies.py` 已将以下固定依赖复制到主结果目录的 `archive/dependencies/objects/`，每个副本均按预期 SHA256 核验；原文件保持原位。

| 归档内容 | 文件数 |
|---|---:|
| 66 单元引用的 FM4PDE 历史结果，包含正式真值和观测掩码 | 2,640 |
| 历史单元输入记录 | 66 |
| 历史输入总目录 | 1 |
| 校准中使用的四个 PDE 的基础与 ControlNet 权重 | 8 |
| 对应模型配置 | 8 |
| 合计 | **2,723** |

副本合计 **27,452,355,947 字节**（约 27.45 GB）。任务以 CPU nice 10 和空闲 I/O 优先级运行，不使用 GPU。文件清单绑定冻结输入目录、四份选定配置以及归档程序版本；每项保留原始绝对路径、归档相对路径、角色、大小和 SHA256。清单为 `archive/dependencies/manifest.json`，SHA256 为 `3c1c634b533b19b94918d5e7e3360b5350be50bbc819bffb96af6ddf17a7523f`；本地副本为 `docs/execution_20260915/archive_dependencies_manifest.json`，对应日志和退出记录也已保存。

恢复时可按清单将内容寻址的副本映射回输入记录中的原始路径；不能仅移动副本而忽略记录中的绝对路径。原训练数据、原模型和其他实验目录仍保留。这份清单只表示固定输入与已有权重的归档通过，不表示正式采样或 Burgers 阶段完成。

初步代码恢复检查也已通过：在本地新建临时空裸仓库，从 `source/archive_dependencies_code.bundle` 独立导入完整历史，运行 `git fsck --full`，确认没有 Git alternates，并恢复正式采样版本 `ae7cd742211afdfbc4ee0859671b9d75fb613283`、Burgers 版本 `ca487a4115bda9df5383a339b4438d1a21a24d43` 和归档版本 `2efdaa134007b302217713387b393dfb21942ac6`。关键源文件与相应原提交逐字节一致，临时恢复仓库已自动清理。检查记录保存在 `docs/execution_20260915/validation_code_bundle_recovery_preliminary.json`；源代码包 SHA256 为 `53f26f02705fa34ef6de2cdc4f1c994c71fa926b27f229cd22f27abdec63eba2`。

最终归档仍须等待全部 66 单元、完整指标和 Burgers 权重齐备后，再保存最终代码包并核验恢复；之后才清理本任务的临时传输包、重复中间文件、临时检出和 tmux 会话。当前各临时检出共享 `code/.git`，不能在完成独立恢复核验前删除它。

2026-09-15 19:17:32 CST 补充运行环境清单：Python 3.12.11、PyTorch `2.8.0+cu128`、CUDA 构建版本 12.8、cuDNN 版本标识 91002、NVIDIA 驱动 580.65.06，两张 A100-SXM4-80GB。两个实际评估进程的可执行文件均为 `fm4pde/bin/python3.12`，工作目录均为冻结的 `code_cell24`，PyTorch 版本与正式批次收据一致。`docs/execution_20260915/runtime_environment_first60.json` 保存采集时间、编译配置、119 个 Python 分发包版本和 30 个 conda 包的构建标识，`runtime_packages_first60.txt` 提供便于查看的版本列表。这是正式评估期间采集的环境信息，不是包含安装包二进制的完整环境镜像；最终恢复还需安装相应依赖。

### 归档内重算与独立目录迁移验证

2026-09-16 02:32 CST，已完成的 46 个单元、46,000 次预测通过只读取归档副本的 CPU 复核：4,976 个文件、24,869,469,966 字节的读取来源均在归档根目录内，全部输入/掩码、观测一致性和 NumPy float64 逐例误差检查通过。记录为 `validation/archive_reader_46cells.json`，绑定固定 46 单元快照。

02:33 CST，另将 Darcy ID 稀疏联合任务与 NS rough 稀疏正问题的 224 个必要文件、约 1.34 GB 数据复制到独立目录，从 Git bundle 恢复源码，在 Python 层禁止旧路径读取后，2,000 例结果仍一致。两个主动旧路径读取探测均被拦截，实际复核未尝试旧路径。临时数据及源码恢复仓库已清理。迁移测试范围为两个单元，不等同于全 66 单元最终恢复检查。

复核入口、命令、源文件标识和证据见 [归档恢复说明](archive_recovery_20260916.md)。该阶段没有重算 PDE 残差、训练损失或 FM4PDE 原始预测；剩余 NS、Burgers 训练/六单元评估及最终完整归档仍按原顺序执行。


### Burgers 观测轴向核查（2026-09-16 03:18 CST）

六个 Burgers 正式输入单元、共 6,000 例完成只读核查。`sensor_column` 的含义已确认是五个固定空间位置的完整时间轨迹，每例 640 个值；张量第一时刻与原始初始条件逐值一致。当前代码与归档 mask 一致，未改变采样协议。旧 ID 文件缺少时间网格与黏性元数据，局限已记录在 [Burgers 协议](burgers_training_protocol_20260915.md)。这项输入审计没有训练模型；Burgers 仍等待前 60 单元完成。

### 后续单元自动收集与复核（2026-09-16 03:53 CST）

`cocogen_first60_report_watch` 已在独立 tmux 中启动，固定检出为 `code_report_watch`，源码提交 `5257f2fbe5dfb1e0940ffc2ef2802ce219244e6d`。任务使用 CPU 四线程、隐藏 GPU、降低调度优先级；不控制模型进程，也不改变采样或训练配置。

每有新的完整单元，流程先运行原指标收集器，保存带时间标识的不可覆盖快照，再对新增单元执行归档输入与预测的独立 NumPy float64 重算。合并旧审计时同时核对每个预测文件哈希与当前快照一致。只有全部已完成单元都有匹配的审计，才发布新的 `reports/metrics_first60_partial.json` 和对应 CSV。

全部 60 个单元出现后，还须等待调度器的 `reports/first60_complete.json`，并通过收集器的完整模式检查。自动收集器不创建或修改这个训练前置标志。其最终状态 `first60_reports_verified` 仅表示第一阶段报告核验完成，仍不代表 Burgers 或整个 66 单元任务完成。

权威进度在 `reports/auto_first60_20260916/state.json`，其中记录当前快照、合并覆盖记录、各独立审计及公开 JSON/CSV 的哈希。逐次快照和审计保存在同一目录，日志为 `logs/first60_report_watch.log`，终止后才生成 `.exit`。正常恢复从最后一次已提交状态继续；未提交的快照保留，后续尝试使用新的文件名。观察器遇到校验错误会退出，由人工检查，不会重启模型。

启动前的真实 50 单元一次性检查已正常退出；随后通过读取已提交状态恢复常驻任务。两次指标快照相同，覆盖 50,000 次完整单元预测和 1,600 个预测文件哈希关联。记录见 `docs/execution_20260915/validation_report_watch_initialization.json`。截至本段时间，新增单元的自动处理路径及最终 60 单元完成路径尚未实际执行，须等相应结果到达后确认。

自动任务运行期间，应读取上述状态及其引用文件，避免同时手工运行收集器写入相同的公开指标文件。原 GPU 采样任务和 Burgers 接续任务继续按冻结顺序运行。
