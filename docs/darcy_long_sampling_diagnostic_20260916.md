# Darcy 长采样与 RePaint 对照测试

## 当前状态

用户于 2026-09-16 要求尝试 2,000 时间步并增加 RePaint。四个独立诊断进程已于 **01:24:23 CST** 启动，在 216 服务器的四张空闲 A800 上运行。197 的四模型正式评估继续执行，正式配置保持冻结。本页当前记录启动协议，尚无完整长采样结论。

## 固定对照

| 配置 | 时间间隔 | 额外 RePaint 次数 | 每样本 NFE |
|---|---:|---:|---:|
| 短配置基线 | 100 | 4 | 500 |
| 增加时间步 | 2,000 | 4 | 10,000 |
| 同时增加重采样 | 2,000 | 10 | 22,000 |

- 使用同一份现有 Darcy ControlNet 权重，checkpoint SHA256 为 `03b6ccbe45a258e38537a006727dd36e54bf60e5e224eaa0cc92edd4188a0b51`。
- 使用 ID 校准样本 **1000–1007**，共八例，覆盖 sparse_forward、sparse_inverse、sparse_joint 三任务，共 **72 次生成**。这些编号已用于先前校准，但没有进入正式 0–999 评估；这是探索性诊断，不是新增独立测试集成绩。
- 每个活动场 500 个观测点。三配置共享样本、有效掩码、权重、训练归一化、随机种子 0 和初始随机场。
- 保留正式配置的最大物理更新量 0.002、最后 50 个时间间隔的修正、10 次后处理、仅更新解通道、Euler 前向重采样和最后预测均值去噪。
- 增加 RePaint 也会增加最后 50 个时间间隔内的物理修正访问次数，因此本测试比较完整采样配置，不能解释为只改变某一种内部运算的严格消融。
- 三配置均在 A800、batch 8、float32、TF32 开启、cuDNN 自动测速关闭的环境中重新运行。基线也在此环境测量，不直接用 A100/batch 32 的正式结果替代。

## 资源与时间

启动前检查显示使用的 GPU 0、2、3、4 均为空闲；其余任务保持运行。batch 1→4→8 的真实网络与物理修正探测通过，batch 8 峰值分配显存约 **1.27 GiB**。该探测只验证显存、有限值和观测一致性，不代表长采样的重建质量。

batch 8 网络调用实测约 **0.03629 秒/次**。按冻结分工计算，最长一路纯网络计算预计 **18.15 分钟**；加入噪声更新、物理修正、加载和 I/O 后，启动时暂估 **20–30 分钟**。后续以真实采样耗时为准。

| GPU / worker | 工作 |
|---|---|
| GPU 0 / worker 0 | 三个短配置基线任务，然后执行 2,000 步 / r=10 的稀疏正问题 |
| GPU 2 / worker 1 | 三个 2,000 步 / r=4 任务 |
| GPU 3 / worker 2 | 2,000 步 / r=10 的稀疏反问题 |
| GPU 4 / worker 3 | 2,000 步 / r=10 的稀疏联合重建 |

216 的系统时钟比参考主机快约 277 秒。用户可见开始时间采用启动收据的 `reference_epoch`；采样时长使用单调时钟，避免跨主机时钟偏差影响耗时计算。

## 结果与验证计划

每个任务保存全部八例预测、输入与模型标识、逐例 a/u 相对 L2、PDE 残差、实际 NFE、采样时间、显存峰值和文件哈希。完整比较须核验九个配置/任务组合、重新计算误差、确认精确观测及初始随机场一致。

效果汇总只对任务目标场计算：正问题为 u，反问题为 a，联合任务先对 a/u 等权平均，再对三任务等权平均。另保留逐样本变化，避免八例平均值掩盖个体差异。

将提供八个样本全部三任务的对照 PDF，以及预先选定的首个样本 1000 的 PNG 预览。图中真值和三配置使用统一的物理量色标，不对各张预测单独增强对比度。

## 路径与代码

197 主归档目录：

```text
/research_data/users/zhangxifeng/C01Python/FM4PDE/outputs/cocogen_main_20260915/diagnostics/darcy_long_sampling_20260916/
```

216 临时执行目录：`/data1/zjinzxf2025/C01Python/cocogen_darcy_long_20260916/`。

源码固定为 `20dfe717fc5df9bbc211dcbccffca01401ec4cfe`。197 的准备检出为 `code_long_diag`；216 的 `code` 从独立 Git bundle 恢复，没有依赖 197 的 Git worktree。

tmux 会话为 `cocogen_darcy_long_gpu0`、`cocogen_darcy_long_gpu2`、`cocogen_darcy_long_gpu3`、`cocogen_darcy_long_gpu4`。日志和退出码保存在诊断目录的 `logs/worker<N>.log`、`worker<N>.exit`。原有数据与正式预测均保持原位。

启动清单 SHA256 为 `f94f4b1c940a1226ec3c34a9fd12d2d7630507bfd70dfd2f61abc0eaa789fb45`。本地启动证据保存于 `docs/execution_20260915/darcy_long_sampling/`，包含 `manifest.json`、`benchmark.json`、`launch.json`；主归档目录已有相同文件与必要输入/权重副本，完成后的预测和对照结果还需回传核验。
